# Phase 7.1: Maestro Emitter Hardening & Traceability - Context

**Gathered:** 2026-04-19
**Status:** Ready for planning

<domain>
## Phase Boundary

Eliminate the code-quality gaps from Phase 7's review and bring REQUIREMENTS.md into verified sync with the shipped implementation. This is a hardening + traceability phase — no new capabilities, no new data models.

**Two of the four fixes already landed** in the Phase 7 REVIEW-FIX cycle (commits 683382e and 4184846). The remaining work is:
1. Confirm diagnostic naming coherence (no orphaned codes)
2. Update REQUIREMENTS.md MAESTRO-01..05 checkboxes to `[x]` and traceability table to "Complete"

</domain>

<decisions>
## Implementation Decisions

### Already Fixed (no re-implementation needed)
- **D-01:** `execFileSync` timeout fix (WR-02) — `timeout: 10_000` is already in `src/editor/commands/emit-maestro.ts`. Do not re-apply.
- **D-02:** `testID` unsafe cast fix (WR-03) — `typeof node.testID === "string" && node.testID.length > 0` guard is already in `src/emit/maestro/step-mapper.ts`. Do not re-apply.
- **D-03:** Golden fixture `ios_permission_flow.ios.yaml` (WR-01) — was already correct at review time. No change needed.

### Diagnostic Naming Audit
- **D-04:** Three diagnostic codes are intentionally distinct and not orphaned:
  - `SPEC_UNRESOLVED_ACTION` (cross-reference.ts, 8 places) — general spec nav/data cross-references
  - `MAESTRO_UNRESOLVED_ACTION` (cross-reference.ts:425) — test flow step references undeclared action
  - `MAESTRO_MISSING_TESTID` (step-mapper.ts) — action exists but no testID found in component tree
  - Verify these are all emitted and none are defined-but-never-emitted. If an orphan is found, either delete it or wire it.

### REQUIREMENTS.md Update
- **D-05:** Flip MAESTRO-01, MAESTRO-02, MAESTRO-03, MAESTRO-04, MAESTRO-05 from `[ ]` to `[x]`.
- **D-06:** Update the traceability table entries for MAESTRO-01..05 from "Pending" to "Complete".
- **D-07:** No other requirement categories need updating — HANDOFF and PI are out of scope.

### Claude's Discretion
- If the diagnostic naming audit reveals a genuine orphan (code defined but never emitted, or emitted but no consumer), Claude may fix it inline. No user decision needed.
- Test coverage: if the existing test suite exercises the already-fixed WR-02/WR-03 paths, no new tests needed. Add a focused test only if coverage of the fixed paths is absent.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Review source
- `.planning/phases/07-maestro-emitter/07-REVIEW.md` — Original findings: WR-01, WR-02, WR-03 and IN-01..IN-03
- `.planning/phases/07-maestro-emitter/07-REVIEW-FIX.md` — Fix report: WR-02 and WR-03 already applied; WR-01 was already correct

### Files to touch
- `src/editor/commands/emit-maestro.ts` — Already fixed (WR-02). Read to confirm, do not re-edit.
- `src/emit/maestro/step-mapper.ts` — Already fixed (WR-03). Read to confirm, do not re-edit.
- `src/model/cross-reference.ts` — Contains `MAESTRO_UNRESOLVED_ACTION` at line 425 and `SPEC_UNRESOLVED_ACTION` at 8 other places. Audit for naming coherence.
- `.planning/REQUIREMENTS.md` — MAESTRO-01..05 checkboxes and traceability table need updating.

### Phase 7 artifacts
- `.planning/phases/07-maestro-emitter/07-VERIFICATION.md` — What was verified for Phase 7
- `.planning/phases/07-maestro-emitter/07-VALIDATION.md` — Nyquist validation record for Phase 7

</canonical_refs>

<code_context>
## Existing Code Insights

### Already-Fixed Code (verify, don't re-touch)
- `src/editor/commands/emit-maestro.ts:33-36` — `execFileSync` with `timeout: 10_000` already present
- `src/emit/maestro/step-mapper.ts:walkForTestID` — `typeof node.testID === "string" && node.testID.length > 0` guards already present

### Diagnostic Code Landscape
- `src/model/cross-reference.ts` — `SPEC_UNRESOLVED_ACTION` (8 uses) + `MAESTRO_UNRESOLVED_SCREEN` (1) + `MAESTRO_UNRESOLVED_ACTION` (1, line 425)
- `src/emit/maestro/step-mapper.ts` — `MAESTRO_MISSING_TESTID` + `MAESTRO_UNRESOLVED_SCREEN`
- All three MAESTRO codes are in the emitter/step-mapper subsystem; SPEC codes are in the core validation pass

### Integration Points
- REQUIREMENTS.md is the only doc file to update; roadmap already shows Phase 7 as `[x]` complete

</code_context>

<specifics>
## Specific Ideas

User chose to skip discussion — phase goal is clear from the review artifacts. All implementation details are fully specified by the success criteria in ROADMAP.md.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 07.1-maestro-emitter-hardening*
*Context gathered: 2026-04-19*
