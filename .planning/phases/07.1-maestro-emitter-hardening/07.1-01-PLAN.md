---
phase: 07.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MAESTRO-01
  - MAESTRO-02
  - MAESTRO-03
  - MAESTRO-04
  - MAESTRO-05

must_haves:
  truths:
    - "REQUIREMENTS.md shows [x] for all five MAESTRO requirements"
    - "REQUIREMENTS.md traceability table shows Complete for MAESTRO-01..05"
    - "WR-02 fix (timeout: 10_000) is confirmed present in emit-maestro.ts"
    - "WR-03 fix (testID type guard) is confirmed present in step-mapper.ts"
    - "No orphaned MAESTRO diagnostic codes exist across the codebase"
    - "Full test suite passes with zero regressions"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated MAESTRO-01..05 checkboxes and traceability rows"
      contains: "[x] **MAESTRO-01**"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 7 implementation"
      via: "traceability table rows MAESTRO-01..05"
      pattern: "MAESTRO-0[1-5].*Complete"
---

<objective>
Confirm the two Phase 7 code-quality fixes are in place, verify no orphaned diagnostic codes
exist in the maestro subsystem, and update REQUIREMENTS.md to reflect that MAESTRO-01..05
have shipped.

Purpose: Close the traceability gap between the verified Phase 7 implementation and the
REQUIREMENTS.md record, which still shows MAESTRO-01..05 as Pending.

Output: Updated REQUIREMENTS.md with [x] checkboxes and Complete traceability rows; full
test suite green; diagnostic naming confirmed coherent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-maestro-emitter-hardening/07.1-CONTEXT.md
@.planning/phases/07.1-maestro-emitter-hardening/07.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm fixes and audit diagnostic naming coherence</name>
  <files>
    src/editor/commands/emit-maestro.ts
    src/emit/maestro/step-mapper.ts
    src/model/cross-reference.ts
  </files>
  <read_first>
    - src/editor/commands/emit-maestro.ts — confirm `timeout: 10_000` is present at the execFileSync call (D-01 / WR-02)
    - src/emit/maestro/step-mapper.ts — confirm `typeof node.testID === "string" && node.testID.length > 0` guard is present in walkForTestID (D-02 / WR-03)
    - src/model/cross-reference.ts — inventory all MAESTRO_* diagnostic codes emitted here (D-04); confirm MAESTRO_UNRESOLVED_SCREEN at line 417 and MAESTRO_UNRESOLVED_ACTION at line 425 are both actively emitted
  </read_first>
  <action>
This is a read-only verification task. Do NOT edit any source file.

Read all three files and confirm the following conditions. If any condition fails, stop and report the discrepancy — do not attempt a silent fix:

1. **emit-maestro.ts WR-02 check**: Grep for `timeout` in `src/editor/commands/emit-maestro.ts`. Expected: `timeout: 10_000` present in the execFileSync options block. The exact value `10_000` (with underscore separator) must be present. Absence → stop and report.

2. **step-mapper.ts WR-03 check**: Grep for `typeof node.testID` in `src/emit/maestro/step-mapper.ts`. Expected: `typeof node.testID === "string" && node.testID.length > 0` present inside the walkForTestID function. Absence → stop and report.

3. **cross-reference.ts diagnostic audit**: Grep for all strings matching `MAESTRO_` in `src/model/cross-reference.ts`. Expected codes (per PATTERNS.md):
   - `MAESTRO_UNRESOLVED_SCREEN` at approximately line 417 — must be in a `ctx.diagnostics.push({...})` call
   - `MAESTRO_UNRESOLVED_ACTION` at approximately line 425 — must be in a `ctx.diagnostics.push({...})` call
   Both must be active emission sites. If either is defined-but-commented-out or referenced only in a string constant without a push call → stop and report.

4. **step-mapper.ts diagnostic audit**: Grep for `MAESTRO_` in `src/emit/maestro/step-mapper.ts`. Expected: `MAESTRO_UNRESOLVED_SCREEN` and `MAESTRO_MISSING_TESTID` both present as active emission sites in `return { ok: false, diagnostic: { code: "..." } }` blocks.

5. **emit-maestro.ts diagnostic audit**: Grep for `MAESTRO_` in `src/editor/commands/emit-maestro.ts`. Expected: `MAESTRO_SYNTAX_ERROR` present as an active emission site.

6. **No orphan check**: Run a global grep for any string matching `"MAESTRO_[A-Z_]+"` across all `.ts` files under `src/`. Collect every unique code found. For each code, verify it appears at least once in a `code:` field within a diagnostic object literal or a `return { ok: false, diagnostic: { code:` block. If any code appears only in a comment or only as a type assertion with no emission site → stop and report.

After confirming all 6 conditions, write a brief audit summary to stdout (not to a file). No files are modified by this task.
  </action>
  <verify>
    <automated>grep -n "timeout" /Users/rayliu/dev/mobile-tui/src/editor/commands/emit-maestro.ts | grep "10_000" && grep -n "typeof node.testID" /Users/rayliu/dev/mobile-tui/src/emit/maestro/step-mapper.ts && grep -n "MAESTRO_" /Users/rayliu/dev/mobile-tui/src/model/cross-reference.ts && grep -n "MAESTRO_" /Users/rayliu/dev/mobile-tui/src/emit/maestro/step-mapper.ts && grep -n "MAESTRO_" /Users/rayliu/dev/mobile-tui/src/editor/commands/emit-maestro.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "timeout" src/editor/commands/emit-maestro.ts` output contains `10_000`
    - `grep -n "typeof node.testID" src/emit/maestro/step-mapper.ts` returns at least one match
    - `grep -n "MAESTRO_UNRESOLVED_SCREEN" src/model/cross-reference.ts` returns at least one match
    - `grep -n "MAESTRO_UNRESOLVED_ACTION" src/model/cross-reference.ts` returns at least one match
    - `grep -n "MAESTRO_MISSING_TESTID" src/emit/maestro/step-mapper.ts` returns at least one match
    - `grep -n "MAESTRO_SYNTAX_ERROR" src/editor/commands/emit-maestro.ts` returns at least one match
    - All MAESTRO_ codes found by global grep have active emission sites (no orphans)
  </acceptance_criteria>
  <done>All six confirmation checks pass; no orphaned MAESTRO diagnostic codes exist; audit summary written to stdout; no source files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md — flip MAESTRO checkboxes and traceability rows</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md — read the full file before editing; locate the MAESTRO requirement lines (lines 69–73) and the traceability table MAESTRO rows (lines 164–168)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` with these two targeted changes:

**Change 1 — Flip MAESTRO checkboxes (lines ~69–73):**

Find these exact lines:
```
- [ ] **MAESTRO-01**: The Maestro emitter is a pure function ...
- [ ] **MAESTRO-02**: Output is two files per flow ...
- [ ] **MAESTRO-03**: Every interaction in the emitted flow selects via a `test:` sigil ...
- [ ] **MAESTRO-04**: Emission validates output via `maestro check-flow-syntax` ...
- [ ] **MAESTRO-05**: A `:emit maestro` command in canvas writes the YAML files ...
```

Replace each `- [ ]` prefix with `- [x]` for all five lines. The rest of each line's text is unchanged. The result must be:
```
- [x] **MAESTRO-01**: The Maestro emitter is a pure function ...
- [x] **MAESTRO-02**: Output is two files per flow ...
- [x] **MAESTRO-03**: Every interaction in the emitted flow selects via a `test:` sigil ...
- [x] **MAESTRO-04**: Emission validates output via `maestro check-flow-syntax` ...
- [x] **MAESTRO-05**: A `:emit maestro` command in canvas writes the YAML files ...
```

**Change 2 — Update traceability table MAESTRO rows (lines ~164–168):**

Find these exact rows in the traceability table:
```
| MAESTRO-01 | Phase 7 | Pending |
| MAESTRO-02 | Phase 7 | Pending |
| MAESTRO-03 | Phase 7 | Pending |
| MAESTRO-04 | Phase 7 | Pending |
| MAESTRO-05 | Phase 7 | Pending |
```

Replace `Pending` with `Complete` in all five rows. The Phase column (`Phase 7`) is unchanged. The result must be:
```
| MAESTRO-01 | Phase 7 | Complete |
| MAESTRO-02 | Phase 7 | Complete |
| MAESTRO-03 | Phase 7 | Complete |
| MAESTRO-04 | Phase 7 | Complete |
| MAESTRO-05 | Phase 7 | Complete |
```

No other lines in REQUIREMENTS.md are modified. Do not change HANDOFF or PI rows. Do not reformat or reorder any other content.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*MAESTRO-0[1-5]\*\*" /Users/rayliu/dev/mobile-tui/.planning/REQUIREMENTS.md && grep -c "MAESTRO-0[1-5].*Complete" /Users/rayliu/dev/mobile-tui/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*MAESTRO-0[1-5]\*\*" .planning/REQUIREMENTS.md` returns 5
    - `grep -c "MAESTRO-0[1-5].*Complete" .planning/REQUIREMENTS.md` returns 5
    - `grep "MAESTRO-0[1-5]" .planning/REQUIREMENTS.md` shows NO lines containing `[ ]` or `Pending`
    - `grep "HANDOFF\|PI-0[1-9]" .planning/REQUIREMENTS.md | grep "Pending"` still returns lines (other Pending rows unchanged)
    - Total line count of REQUIREMENTS.md is unchanged (no accidental insertions or deletions)
  </acceptance_criteria>
  <done>REQUIREMENTS.md has [x] for MAESTRO-01..05 in the requirements list and "Complete" for all five in the traceability table; all other rows are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite gate</name>
  <files></files>
  <read_first>
    - package.json — confirm the test script invocation (typically `vitest run` or `npm test`)
  </read_first>
  <action>
Run the full test suite to confirm no regressions from the REQUIREMENTS.md edit (which is doc-only, but must be confirmed):

```
npx vitest run
```

The suite must exit 0. The baseline before this phase was 968 pass / 1 skip (from STATE.md: "968 pass / 1 skip" for Phase 7 final state). The expected result after this plan is the same count: **968 pass / 1 skip** (no new tests in this phase, no source changes).

If any test fails:
- Report the failing test name and error message
- Do not attempt to fix failing tests — this plan makes no source code changes; a test failure indicates an unexpected environmental issue

Also run TypeScript type-check to confirm no type errors:
```
npx tsc --noEmit
```

Expected: 0 errors.
  </action>
  <verify>
    <automated>cd /Users/rayliu/dev/mobile-tui && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0
    - Output contains a line matching `968 passed` (or higher — if prior phases added tests)
    - Output shows at most `1 skipped` (the intentionally-skipped MAESTRO_CLI integration test)
    - Output shows `0 failed`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Full test suite passes: 968 pass / 1 skip / 0 fail; tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk read | REQUIREMENTS.md is read from and written to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Tampering | .planning/REQUIREMENTS.md edit | accept | Documentation-only file; no code execution path; no PII; git-tracked and diff-reviewable. Low risk — incorrect edit is immediately visible in git diff. |
| T-07.1-02 | Information Disclosure | Diagnostic code audit (grep across src/) | accept | Read-only grep over local source files; no external I/O; no secrets in diagnostic code strings; grep output stays in executor context only. |
| T-07.1-03 | Denial of Service | execFileSync in emit-maestro.ts | mitigate | WR-02 fix already applied: `timeout: 10_000` prevents indefinite block on JVM cold start. Confirmed in Task 1. |

No new attack surfaces introduced by this phase (documentation update only). Security posture is unchanged from Phase 7.
</threat_model>

<verification>
After Task 3 completes, the following must be true:

1. `grep "[x] **MAESTRO-01**" .planning/REQUIREMENTS.md` returns a match
2. `grep "MAESTRO-05.*Complete" .planning/REQUIREMENTS.md` returns a match
3. `grep "timeout.*10_000" src/editor/commands/emit-maestro.ts` returns a match
4. `grep "typeof node.testID" src/emit/maestro/step-mapper.ts` returns a match
5. `npx vitest run` exits 0 with 968 pass / 1 skip
</verification>

<success_criteria>
- All five MAESTRO requirements are `[x]` in REQUIREMENTS.md requirement list
- All five MAESTRO traceability rows show "Complete"
- No orphaned MAESTRO diagnostic codes exist in the codebase
- WR-02 (timeout) and WR-03 (testID guard) confirmed present in source
- Full test suite: 968 pass / 1 skip / 0 fail
- tsc --noEmit: 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-maestro-emitter-hardening/07.1-01-SUMMARY.md`
</output>
