---
phase: 03-wireframe-renderer-dogfood-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emit/wireframe/index.ts
  - src/emit/wireframe/dispatch.ts
  - src/emit/wireframe/components/button.ts
  - src/emit/wireframe/components/toggle.ts
  - src/emit/wireframe/components/text-field.ts
  - src/emit/wireframe/components/segmented-control.ts
  - src/emit/wireframe/components/text.ts
  - src/emit/wireframe/components/icon.ts
  - src/emit/wireframe/components/divider.ts
  - src/emit/wireframe/components/spacer.ts
  - src/emit/wireframe/components/image.ts
  - src/emit/wireframe/components/column.ts
  - src/emit/wireframe/components/row.ts
  - src/emit/wireframe/components/card.ts
  - src/emit/wireframe/components/list.ts
  - src/emit/wireframe/components/list-item.ts
  - src/emit/wireframe/components/nav-bar.ts
  - src/emit/wireframe/components/tab-bar.ts
  - src/emit/wireframe/components/modal.ts
  - src/emit/wireframe/components/sheet.ts
  - src/emit/wireframe/layout.ts
  - src/emit/wireframe/text-style.ts
  - src/emit/wireframe/overflow.ts
  - src/emit/wireframe/variants.ts
  - scripts/render-wireframe.ts
  - fixtures/wireframes/README.md
  - fixtures/wireframes/SHARED.md
  - tests/wireframe-ascii-baseline.test.ts
  - tests/wireframe-catalog.test.ts
  - tests/dogfood-gate.test.ts
  - .gitattributes
  - package.json
autonomous: true
requirements: [WIREFRAME-01, WIREFRAME-02, WIREFRAME-03, WIREFRAME-05, WIREFRAME-06]

must_haves:
  truths:
    - "Directory src/emit/wireframe/ exists and contains stubs for all 18 emitter modules"
    - "Every emitter stub throws 'NYI' when invoked; dispatch switch enumerates all 18 kinds with a never fallback"
    - "Integration test files tests/wireframe-ascii-baseline.test.ts + tests/wireframe-catalog.test.ts + tests/dogfood-gate.test.ts exist and are RED (skipped or xfail) per Nyquist"
    - "scripts/render-wireframe.ts exists as the CLI entry and throws NotImplemented when invoked"
    - ".gitattributes forces LF line endings on *.wf.txt; package.json has an npm run wireframe script alias"
    - "npx tsc --noEmit + npx biome check . exit 0 with the new scaffold in place (no Phase 1/2 regression)"
  artifacts:
    - path: "src/emit/wireframe/index.ts"
      provides: "Public barrel exporting render() + renderAllVariants() stubs"
      contains: "export { render"
    - path: "src/emit/wireframe/dispatch.ts"
      provides: "renderNode exhaustive-switch dispatcher over 18 kinds with never fallback"
      contains: "const _exhaustive: never = node"
    - path: "src/emit/wireframe/components/button.ts"
      provides: "renderButton stub throwing NYI"
      contains: "export function renderButton"
    - path: "src/emit/wireframe/components/sheet.ts"
      provides: "renderSheet stub throwing NYI"
      contains: "export function renderSheet"
    - path: "scripts/render-wireframe.ts"
      provides: "CLI stub that argv-parses and throws NotImplemented"
      contains: "process.argv.slice(2)"
    - path: "tests/wireframe-ascii-baseline.test.ts"
      provides: "WIREFRAME-02 regex enforcement harness"
      contains: "ASCII_BASELINE"
    - path: "tests/wireframe-catalog.test.ts"
      provides: "WIREFRAME-03 catalog coverage harness"
      contains: "COMPONENT_KINDS"
    - path: "tests/dogfood-gate.test.ts"
      provides: "WIREFRAME-06 20-file count + SHARED.md parser harness"
      contains: "SHARED.md"
    - path: ".gitattributes"
      provides: "LF line-ending lock for *.wf.txt"
      contains: "*.wf.txt text eol=lf"
    - path: "package.json"
      provides: "wireframe npm script alias"
      contains: "\"wireframe\""
  key_links:
    - from: "src/emit/wireframe/index.ts"
      to: "src/emit/wireframe/variants.ts"
      via: "re-export render + renderAllVariants"
      pattern: "export \\{ .*render.* \\} from \"\\./variants"
    - from: "src/emit/wireframe/dispatch.ts"
      to: "src/emit/wireframe/components/*.ts"
      via: "import every renderX function and wire into switch cases"
      pattern: "import \\{ render.* \\} from \"\\./components/"
    - from: "scripts/render-wireframe.ts"
      to: "src/serialize/index.ts"
      via: "parseSpecFile import"
      pattern: "parseSpecFile"
---

<objective>
Stand up the Phase-3 wireframe renderer skeleton: the complete file layout for src/emit/wireframe/ (barrel, dispatch, 18 emitter stubs, layout, text-style, overflow, variants all as NYI stubs), the CLI entry as a stub, the three integration-test harnesses (ascii-baseline, catalog, dogfood-gate) as RED-or-skipped, the fixture corpus directory scaffolds (README.md + SHARED.md template), and the repo-level wiring (`.gitattributes` + `package.json` npm script). Downstream plans fill in implementations against this skeleton — every Wave 2/3 plan can run parallel because every file this plan creates is owned, stubbed, and compile-green from the outset.

Purpose: establish a single Wave 0 that creates the layer boundary, barrel surface, exhaustive-switch gate, and test scaffolds. Downstream emitter plans (03-04..07) only modify their own emitter files; they never co-edit the dispatcher (wired once here) or the barrel (wired once here). Zero shared-file collisions for parallel execution.

Output: complete directory tree under `src/emit/wireframe/`, `scripts/` (new dir), `fixtures/wireframes/` (new dir with README.md + SHARED.md template), 3 new test files under `tests/`, edited `.gitattributes` + `package.json`. No new runtime dependencies — per RESEARCH §Standard Stack zero-new-deps guarantee.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-wireframe-renderer-dogfood-gate/03-CONTEXT.md
@.planning/phases/03-wireframe-renderer-dogfood-gate/03-RESEARCH.md
@.planning/phases/03-wireframe-renderer-dogfood-gate/03-PATTERNS.md
@src/model/component.ts
@src/model/screen.ts
@src/model/variant.ts
@src/serialize/parse.ts
@src/serialize/index.ts
@src/primitives/path.ts
@.gitattributes
@package.json
@vitest.config.ts

<interfaces>
From src/model/component.ts (authoritative catalog + ComponentNode union):

```typescript
export const COMPONENT_KINDS = [
  "Column", "Row", "Text", "Button", "TextField", "List", "ListItem",
  "Card", "Image", "Icon", "Divider", "Toggle", "SegmentedControl",
  "TabBar", "NavBar", "Modal", "Sheet", "Spacer",
] as const;
export type ComponentKind = (typeof COMPONENT_KINDS)[number];

export type ComponentNode =
  | { kind: "Text"; text: string; style?: "heading-1" | "heading-2" | "body" | "caption" }
  | { kind: "Icon"; name: string }
  | { kind: "Divider" }
  | { kind: "Spacer"; size?: "sm" | "md" | "lg" }
  | { kind: "Image"; source: string; alt: string }
  | { kind: "Button"; label: string; action: string; testID: string;
      variant?: "primary" | "secondary" | "text" }
  | { kind: "TextField"; label: string; action: string; testID: string;
      placeholder?: string; bindsTo?: string }
  | { kind: "Toggle"; label: string; action: string; testID: string; bindsTo?: string }
  | { kind: "SegmentedControl"; label: string; action: string; testID: string;
      options: string[]; bindsTo?: string }
  | { kind: "Column"; children: ComponentNode[];
      align?: "start"|"center"|"end"; gap?: "sm"|"md"|"lg" }
  | { kind: "Row"; children: ComponentNode[];
      align?: "start"|"center"|"end"; gap?: "sm"|"md"|"lg" }
  | { kind: "Card"; child: ComponentNode }
  | { kind: "List"; itemTemplate: ComponentNode; bindsTo: string }
  | { kind: "ListItem"; children: ComponentNode[];
      label?: string; action?: string; testID?: string }
  | { kind: "NavBar"; title: string; leading?: ComponentNode; trailing?: ComponentNode }
  | { kind: "TabBar"; items: Array<{ label: string; action: string; testID: string; icon?: string }> }
  | { kind: "Modal"; child: ComponentNode }
  | { kind: "Sheet"; child: ComponentNode };
```

From src/serialize/index.ts (Phase-3 consumes parseSpecFile):

```typescript
export { type ParseResult, parseSpecFile } from "./parse.ts";
// ParseResult = { spec: Spec | null; astHandle: AstHandle | null; diagnostics: Diagnostic[]; body: string }
```

From src/primitives/diagnostic.ts (for [BROKEN LINK] marker later):

```typescript
export interface Diagnostic { code: string; severity: "error"|"warning"|"info"; path: string; message: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — integration-test harness skeletons + fixture corpus scaffolds</name>
  <files>
    tests/wireframe-ascii-baseline.test.ts,
    tests/wireframe-catalog.test.ts,
    tests/dogfood-gate.test.ts,
    fixtures/wireframes/README.md,
    fixtures/wireframes/SHARED.md,
    .gitattributes
  </files>
  <read_first>
    - .planning/phases/03-wireframe-renderer-dogfood-gate/03-RESEARCH.md (Pattern 1/2, §Common Pitfalls 2, Open Question 5 for SHARED.md shape)
    - .planning/phases/03-wireframe-renderer-dogfood-gate/03-PATTERNS.md (§tests/wireframe-ascii-baseline.test.ts analog `tests/fidelity.test.ts`; §tests/dogfood-gate.test.ts analog `tests/fixtures.test.ts` + `tests/round-trip.test.ts`)
    - .planning/phases/03-wireframe-renderer-dogfood-gate/03-CONTEXT.md (D-46, D-47, D-48, D-49)
    - tests/catalog-coverage.test.ts (existing analog for COMPONENT_KINDS walker)
    - tests/round-trip.test.ts lines 74-78 (existing analog for `FIXTURES.length === 20` assertion shape)
    - tests/fidelity.test.ts (existing analog for readFileSync + walk pattern)
    - .gitattributes (current state — already locks *.spec.md to LF; Phase 3 extends to *.wf.txt)
  </read_first>
  <behavior>
    - Test 1: tests/wireframe-ascii-baseline.test.ts globs fixtures/wireframes/**/*.wf.txt and asserts each matches /^[|\-+. \x20-\x7E\n]*$/ (per D-33 glyph palette + D-38 60-col + WIREFRAME-02). At Wave-0 time the fixtures dir is empty so the test passes trivially (zero files matched, regex satisfied vacuously).
    - Test 2: tests/wireframe-catalog.test.ts imports COMPONENT_KINDS from src/model/component.ts and parses 3 canonical fixtures (habit-tracker, todo, social-feed) via parseSpecFile; asserts every kind in COMPONENT_KINDS appears in at least one fixture's walker output (this test runs NOW because fixtures exist + Phase-1 walker works; the RENDER step is deferred — marked .skip or wrapped in xfail until 03-08 wires render()). Stub a placeholder render-assert block that's .skip-marked with a comment `// UNSKIP after Plan 03-08 ships variants.ts`.
    - Test 3: tests/dogfood-gate.test.ts counts .wf.txt files under fixtures/wireframes/; at Wave-0 the count is 0 — assertion is .skip-marked `// UNSKIP after Plan 03-09 authors 20 fixtures`. Second assertion reads fixtures/wireframes/SHARED.md and parses via gray-matter + yaml as per RESEARCH Open Q 5 recommendation — also .skip until 03-09 seeds SHARED.md content.
    - .gitattributes: add line `*.wf.txt text eol=lf` so Windows contributors don't regress LF bytes (per RESEARCH Pitfall 2 + Phase-2 .gitattributes precedent for *.spec.md).
    - fixtures/wireframes/README.md: placeholder "# Phase 3 reference wireframes (corpus of 20 forthcoming)" header; 03-09 fills in the 20-entry index.
    - fixtures/wireframes/SHARED.md: YAML-frontmatter template with `schema: mobile-tui/shared/1` + empty `shared: []` list + markdown body explaining the gate per D-48 + D-49.
  </behavior>
  <action>
    CREATE tests/wireframe-ascii-baseline.test.ts with this exact structure:
    ```typescript
    // Tests that every fixtures/wireframes/**/*.wf.txt file contains ONLY the
    // ASCII-baseline character set (WIREFRAME-02 + D-33 glyph palette at 60-col).
    // Permitted bytes: | - + . space, plus printable ASCII 0x20-0x7E, plus LF (0x0A).
    // UNICODE BMP box-drawing glyphs allowed ONLY in Phase-5 TUI preview, never here.
    import { readFileSync } from "node:fs";
    import { readdir } from "node:fs/promises";
    import { join, resolve } from "node:path";
    import { describe, expect, it } from "vitest";

    const ASCII_BASELINE = /^[|\-+. \x20-\x7E\n]*$/;
    const WIREFRAME_ROOT = resolve("fixtures/wireframes");

    export async function allWireframeFiles(): Promise<string[]> {
      const out: string[] = [];
      async function walk(dir: string): Promise<void> {
        let entries;
        try {
          entries = await readdir(dir, { withFileTypes: true });
        } catch {
          return; // dir may not exist yet at Wave-0
        }
        for (const e of entries) {
          const abs = join(dir, e.name);
          if (e.isDirectory()) await walk(abs);
          else if (e.isFile() && e.name.endsWith(".wf.txt")) out.push(abs);
        }
      }
      await walk(WIREFRAME_ROOT);
      return out;
    }

    describe("wireframe ASCII-baseline (WIREFRAME-02)", () => {
      it("every .wf.txt file matches ^[|\\-+. \\x20-\\x7E\\n]*$", async () => {
        const files = await allWireframeFiles();
        // At Wave-0 files.length === 0; later plans grow it to 20.
        for (const f of files) {
          const content = readFileSync(f, "utf8");
          if (!ASCII_BASELINE.test(content)) {
            const bad = [...content].filter((c) => !/[|\-+. \x20-\x7E\n]/.test(c));
            console.error(`[${f}] non-ASCII-baseline chars:`, JSON.stringify(bad));
            throw new Error(`non-ASCII-baseline chars in ${f}`);
          }
        }
      });
    });
    ```

    CREATE tests/wireframe-catalog.test.ts with COMPONENT_KINDS walker from canonical fixtures; mark render-snapshot block as .skip with UNSKIP comment (see behavior). Use parseSpecFile from src/serialize/index.ts. Walk each canonical fixture; assert for each COMPONENT_KIND value the walker encounters it in at least one of the three canonicals. Use an inner `it.skip("renders every kind via render() — UNSKIP after Plan 03-08", ...)` stanza to preserve the test ID.

    CREATE tests/dogfood-gate.test.ts with two describe blocks:
      (a) describe("dogfood gate — file count (WIREFRAME-06)") — `it.skip("fixtures/wireframes/ contains exactly 20 .wf.txt files — UNSKIP after Plan 03-09", ...)`
      (b) describe("dogfood gate — SHARED.md evidence (D-48)") — `it.skip("SHARED.md parses with schema: mobile-tui/shared/1 and ≥3 shareable verdicts — UNSKIP after Plan 03-09", ...)`
    Both stanzas include the full (non-skipped) implementation body inside the skip so grep checks find the literal `SHARED.md` and `mobile-tui/shared/1` strings.

    CREATE fixtures/wireframes/README.md:
    ```markdown
    # Phase 3 reference wireframes

    This directory holds the 20 golden ASCII wireframes that gate Phase 4.
    File layout (D-47): `{fixture_slug}/{screen_id}-{variant}.wf.txt`.
    Authoritative shape: D-33..D-49 in `.planning/phases/03-wireframe-renderer-dogfood-gate/03-CONTEXT.md`.

    Planner authors the 20-entry index in Plan 03-09.
    ```

    CREATE fixtures/wireframes/SHARED.md (template, to be populated during Wave 4 human gate):
    ```markdown
    ---
    schema: mobile-tui/shared/1
    shared: []
    ---

    # Dogfood evidence sidecar (D-48)

    This file records ≥3 shareable-verdict entries before Phase 4 planning can start (D-49).
    Each entry:
      - `screen`: "{fixture_slug}/{screen_id}-{variant}" identifier
      - `target`: URL of the PR / Slack thread / issue where the wireframe was pasted
      - `date`: ISO date
      - `verdict`: "shareable" or "needs-work"
      - `notes`: free-form prose
    ```

    EDIT .gitattributes (append, preserving existing content):
    ```
    # Phase 3 wireframe corpus — lock to LF so Windows contributors don't
    # regress LF bytes (tests/wireframe-ascii-baseline.test.ts assumes LF).
    *.wf.txt text eol=lf
    fixtures/wireframes/**/*.wf.txt text eol=lf
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/wireframe-ascii-baseline.test.ts tests/wireframe-catalog.test.ts tests/dogfood-gate.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/wireframe-ascii-baseline.test.ts` returns 0
    - `test -f tests/wireframe-catalog.test.ts` returns 0
    - `test -f tests/dogfood-gate.test.ts` returns 0
    - `test -d fixtures/wireframes` returns 0
    - `test -f fixtures/wireframes/README.md` returns 0
    - `test -f fixtures/wireframes/SHARED.md` returns 0
    - `grep -q "ASCII_BASELINE" tests/wireframe-ascii-baseline.test.ts` returns 0
    - `grep -q "COMPONENT_KINDS" tests/wireframe-catalog.test.ts` returns 0
    - `grep -q "SHARED.md" tests/dogfood-gate.test.ts` returns 0
    - `grep -q "schema: mobile-tui/shared/1" fixtures/wireframes/SHARED.md` returns 0
    - `grep -q "\\*\\.wf\\.txt text eol=lf" .gitattributes` returns 0
    - `npx vitest run tests/wireframe-ascii-baseline.test.ts tests/wireframe-catalog.test.ts tests/dogfood-gate.test.ts` exits 0 (harness files compile; skipped tests report as pending, ascii-baseline and catalog-walker green)
    - `npx tsc --noEmit` exits 0 with no new errors
    - `npx biome check .` exits 0 with no new errors
  </acceptance_criteria>
  <done>All three integration-test files exist and are compilable; catalog-walker + ascii-baseline tests pass; dogfood-gate tests are .skip with a documented UNSKIP comment; fixtures/wireframes directory tree scaffolded with README + SHARED.md template; .gitattributes extended to cover *.wf.txt.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — src/emit/wireframe scaffolding (18 emitter stubs + dispatch + barrel + layout/text-style/overflow/variants stubs)</name>
  <files>
    src/emit/wireframe/index.ts,
    src/emit/wireframe/dispatch.ts,
    src/emit/wireframe/layout.ts,
    src/emit/wireframe/text-style.ts,
    src/emit/wireframe/overflow.ts,
    src/emit/wireframe/variants.ts,
    src/emit/wireframe/components/text.ts,
    src/emit/wireframe/components/icon.ts,
    src/emit/wireframe/components/divider.ts,
    src/emit/wireframe/components/spacer.ts,
    src/emit/wireframe/components/image.ts,
    src/emit/wireframe/components/button.ts,
    src/emit/wireframe/components/text-field.ts,
    src/emit/wireframe/components/toggle.ts,
    src/emit/wireframe/components/segmented-control.ts,
    src/emit/wireframe/components/column.ts,
    src/emit/wireframe/components/row.ts,
    src/emit/wireframe/components/card.ts,
    src/emit/wireframe/components/list.ts,
    src/emit/wireframe/components/list-item.ts,
    src/emit/wireframe/components/nav-bar.ts,
    src/emit/wireframe/components/tab-bar.ts,
    src/emit/wireframe/components/modal.ts,
    src/emit/wireframe/components/sheet.ts
  </files>
  <read_first>
    - .planning/phases/03-wireframe-renderer-dogfood-gate/03-PATTERNS.md (§src/emit/wireframe/index.ts EXPLICIT-NAMED barrel; §renderNode exhaustive-switch; §components/*.ts per-kind Extract<ComponentNode, { kind: ... }> narrowing)
    - .planning/phases/03-wireframe-renderer-dogfood-gate/03-RESEARCH.md (Pattern 4 Exhaustive-Kind Switch, lines 412-445; §Architecture Patterns src/emit/wireframe/ file tree)
    - src/serialize/index.ts (EXPLICIT-NAMED barrel precedent — top comment block + `export { type X, fn } from "./leaf.ts"` shape)
    - src/model/component.ts (ComponentNode union — every emitter narrows via `Extract<ComponentNode, { kind: "Foo" }>`; COMPONENT_KINDS tuple order)
    - src/model/cross-reference.ts lines 64-117 (visitNode exhaustive switch — the canonical Phase-1 analog for renderNode)
  </read_first>
  <behavior>
    - Every emitter stub exports exactly one function `renderX(node: Extract<ComponentNode, { kind: "X" }>, width: number): string[]` with a body that throws `new Error("NYI: Plan 03-NN")`. Planner replaces NYI tag with correct plan number (03-04 for leaves, 03-05 for interactables, 03-06 for structural, 03-07 for chrome+overlays) so later executors land in the right plan.
    - dispatch.ts exports `renderNode(node: ComponentNode, width: number): string[]` with a fully-populated switch covering all 18 kinds, each calling the corresponding `renderX` imported from ./components/*; default branch includes `const _exhaustive: never = node` compile-time gate plus runtime `throw new Error(\`unreachable: ${...}\`)`.
    - layout.ts, text-style.ts, overflow.ts, variants.ts each export signature stubs that later plans fill in; all throw `new Error("NYI: Plan 03-NN")` to keep tsc + biome green.
    - index.ts uses EXPLICIT-NAMED exports per Phase-2 barrel pattern; top comment block matches src/serialize/index.ts shape; re-exports `render`, `renderAllVariants` (from variants.ts), `PHONE_WIDTH` (from layout.ts). Internal emitters NOT re-exported.
    - Compile cleanliness: `npx tsc --noEmit` passes even though bodies throw — stubs are typed correctly; the exhaustive-switch `never` check holds.
  </behavior>
  <action>
    CREATE src/emit/wireframe/ directory tree. For EACH of the 18 component kinds, CREATE a file under src/emit/wireframe/components/{kebab-kind}.ts with this EXACT shape:

    ```typescript
    // src/emit/wireframe/components/button.ts
    // renderButton — D-34 glyph alphabet (primary [[ ]], secondary [ ], text bare).
    // Implementation lands in Plan 03-05.
    import type { ComponentNode } from "../../../model/component.ts";

    export function renderButton(
      node: Extract<ComponentNode, { kind: "Button" }>,
      width: number,
    ): string[] {
      void node;
      void width;
      throw new Error("NYI: Plan 03-05 renderButton (scaffolded in 03-01)");
    }
    ```

    Use these NYI tags per-kind so executors find the right follow-up plan:
    - Plan 03-04 (leaves): text.ts, icon.ts, divider.ts, spacer.ts, image.ts
    - Plan 03-05 (interactables): button.ts, toggle.ts, text-field.ts, segmented-control.ts
    - Plan 03-06 (structural): column.ts, row.ts, card.ts, list.ts, list-item.ts
    - Plan 03-07 (chrome + overlays): nav-bar.ts, tab-bar.ts, modal.ts, sheet.ts

    For each of text.ts, icon.ts, divider.ts, spacer.ts, image.ts, button.ts, text-field.ts, toggle.ts, segmented-control.ts, column.ts, row.ts, card.ts, list.ts, list-item.ts, nav-bar.ts, tab-bar.ts, modal.ts, sheet.ts — export a typed renderX stub matching the per-kind Extract<ComponentNode, { kind: "X" }> signature per D-34..D-36 + Phase-3 PATTERNS.md §components/*.ts.

    CREATE src/emit/wireframe/dispatch.ts (THE exhaustive-switch gate — single file, lands once):
    ```typescript
    // src/emit/wireframe/dispatch.ts
    // renderNode — exhaustive-switch dispatcher over all 18 component kinds.
    // Adding a kind to COMPONENT_KINDS breaks compilation HERE until a case
    // is added — the `never` fallback is the compile-time gate per
    // Phase 1 D-01 + RESEARCH §Pattern 4.
    import type { ComponentNode } from "../../model/component.ts";
    import { renderButton } from "./components/button.ts";
    import { renderCard } from "./components/card.ts";
    import { renderColumn } from "./components/column.ts";
    import { renderDivider } from "./components/divider.ts";
    import { renderIcon } from "./components/icon.ts";
    import { renderImage } from "./components/image.ts";
    import { renderList } from "./components/list.ts";
    import { renderListItem } from "./components/list-item.ts";
    import { renderModal } from "./components/modal.ts";
    import { renderNavBar } from "./components/nav-bar.ts";
    import { renderRow } from "./components/row.ts";
    import { renderSegmentedControl } from "./components/segmented-control.ts";
    import { renderSheet } from "./components/sheet.ts";
    import { renderSpacer } from "./components/spacer.ts";
    import { renderTabBar } from "./components/tab-bar.ts";
    import { renderText } from "./components/text.ts";
    import { renderTextField } from "./components/text-field.ts";
    import { renderToggle } from "./components/toggle.ts";

    export function renderNode(node: ComponentNode, width: number): string[] {
      switch (node.kind) {
        case "Text": return renderText(node, width);
        case "Icon": return renderIcon(node, width);
        case "Divider": return renderDivider(node, width);
        case "Spacer": return renderSpacer(node, width);
        case "Image": return renderImage(node, width);
        case "Button": return renderButton(node, width);
        case "TextField": return renderTextField(node, width);
        case "Toggle": return renderToggle(node, width);
        case "SegmentedControl": return renderSegmentedControl(node, width);
        case "Column": return renderColumn(node, width);
        case "Row": return renderRow(node, width);
        case "Card": return renderCard(node, width);
        case "List": return renderList(node, width);
        case "ListItem": return renderListItem(node, width);
        case "NavBar": return renderNavBar(node, width);
        case "TabBar": return renderTabBar(node, width);
        case "Modal": return renderModal(node, width);
        case "Sheet": return renderSheet(node, width);
        default: {
          const _exhaustive: never = node;
          throw new Error(`unreachable: ${(_exhaustive as { kind: string }).kind}`);
        }
      }
    }
    ```

    CREATE src/emit/wireframe/layout.ts with signature-only stubs (implementations land in Plan 03-02):
    ```typescript
    // src/emit/wireframe/layout.ts
    // Fixed-60-col frame composer + border drawing + content padding.
    // Width arithmetic documented per RESEARCH Pitfall 4 in Plan 03-02.
    //
    // Scope:
    //   - PHONE_WIDTH: the fixed outer width (D-38 = 60)
    //   - buildVariantHeader(screenId, kind, whenExpr, width) — merges block
    //     header into top border (D-40); two-stage overflow (try --+, then -+,
    //     then truncate) per RESEARCH Pitfall 5.
    //   - padRight(str, width): string — right-pads with spaces, truncates
    //     via overflow.ts if overlong.
    //   - drawFrame(bodyLines, width): string[] — wraps body in `| ... |`
    //     content rows + top/bottom `+--+` borders.
    //
    // Implementation lands in Plan 03-02.

    export const PHONE_WIDTH = 60;
    export type VariantKind = "content" | "empty" | "loading" | "error";

    export function buildVariantHeader(
      screenId: string,
      kind: VariantKind,
      whenExpr: string | undefined,
      width: number,
    ): string {
      void screenId; void kind; void whenExpr; void width;
      throw new Error("NYI: Plan 03-02 buildVariantHeader");
    }

    export function padRight(str: string, width: number): string {
      void str; void width;
      throw new Error("NYI: Plan 03-02 padRight");
    }

    export function drawFrame(bodyLines: string[], width: number): string[] {
      void bodyLines; void width;
      throw new Error("NYI: Plan 03-02 drawFrame");
    }
    ```

    CREATE src/emit/wireframe/text-style.ts:
    ```typescript
    // src/emit/wireframe/text-style.ts
    // Text.style → ASCII mapping per D-43.
    //   heading-1 → ALL CAPS
    //   heading-2 → Title Case (respect author capitalization if mixed)
    //   body → plain
    //   caption → wrapped in ( )
    // Implementation lands in Plan 03-03.

    export type TextStyle = "heading-1" | "heading-2" | "body" | "caption";

    export function applyTextStyle(text: string, style?: TextStyle): string {
      void text; void style;
      throw new Error("NYI: Plan 03-03 applyTextStyle");
    }
    ```

    CREATE src/emit/wireframe/overflow.ts:
    ```typescript
    // src/emit/wireframe/overflow.ts
    // truncateWithEllipsis per D-44. Deterministic 1-line rows; no soft-wrap.
    // The `[BROKEN LINK]` marker is load-bearing per RESEARCH Pitfall 6 —
    // other content gives way first.
    // Implementation lands in Plan 03-03.

    export function truncate(str: string, width: number): string {
      void str; void width;
      throw new Error("NYI: Plan 03-03 truncate");
    }
    ```

    CREATE src/emit/wireframe/variants.ts:
    ```typescript
    // src/emit/wireframe/variants.ts
    // Top-level renderer composition — stacks 4 variants per D-39; null
    // variants render as 1-line (N/A) marker frames per D-39; acceptance
    // footer appends under content only per D-45.
    //
    // Implementation lands in Plan 03-08.
    import type { Diagnostic } from "../../primitives/diagnostic.ts";
    import type { Spec } from "../../model/index.ts";

    export interface RenderOptions {
      diagnostics?: Diagnostic[];
    }

    export function render(spec: Spec, screenId: string, opts?: RenderOptions): string {
      void spec; void screenId; void opts;
      throw new Error("NYI: Plan 03-08 render");
    }

    export function renderAllVariants(spec: Spec, screenId: string, opts?: RenderOptions): string {
      // Alias / convenience — same shape as render for v1. Plan 03-08 may
      // unify these into a single export.
      return render(spec, screenId, opts);
    }
    ```

    CREATE src/emit/wireframe/index.ts (EXPLICIT-NAMED barrel per src/serialize/index.ts precedent):
    ```typescript
    // src/emit/wireframe/index.ts — public barrel for the L4 Emit layer
    // (wireframe sub-module). Consumers: scripts/render-wireframe.ts (Phase 3
    // CLI), Phase 4 editor-store preview pane, Phase 5 canvas preview pane,
    // Phase 8 :yank wireframe handler.
    //
    // Implementation details (dispatch.renderNode, per-kind emitters,
    // layout primitives, text-style, overflow) are INTERNAL and imported
    // directly from leaf modules by co-located tests.
    //
    // Barrel follows the EXPLICIT-NAMED pattern per src/model/index.ts +
    // src/serialize/index.ts (NOT `export *`).
    export { PHONE_WIDTH, type VariantKind } from "./layout.ts";
    export { type RenderOptions, render, renderAllVariants } from "./variants.ts";
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx biome check src/emit/wireframe</automated>
  </verify>
  <acceptance_criteria>
    - `test -d src/emit/wireframe/components` returns 0
    - `ls src/emit/wireframe/components/*.ts | wc -l` returns 18
    - `test -f src/emit/wireframe/index.ts` returns 0
    - `test -f src/emit/wireframe/dispatch.ts` returns 0
    - `test -f src/emit/wireframe/layout.ts` returns 0
    - `test -f src/emit/wireframe/text-style.ts` returns 0
    - `test -f src/emit/wireframe/overflow.ts` returns 0
    - `test -f src/emit/wireframe/variants.ts` returns 0
    - `grep -q "const _exhaustive: never = node" src/emit/wireframe/dispatch.ts` returns 0
    - `grep -cE "^\s*case \"" src/emit/wireframe/dispatch.ts` returns 18 (one per kind)
    - `grep -q "export function render" src/emit/wireframe/variants.ts` returns 0
    - `grep -q "PHONE_WIDTH = 60" src/emit/wireframe/layout.ts` returns 0
    - `grep -q "applyTextStyle" src/emit/wireframe/text-style.ts` returns 0
    - `grep -q "export function truncate" src/emit/wireframe/overflow.ts` returns 0
    - Barrel index.ts NEVER uses `export *` — `! grep -E "^export \\*" src/emit/wireframe/index.ts` returns 0
    - `npx tsc --noEmit` exits 0
    - `npx biome check src/emit/wireframe` exits 0
  </acceptance_criteria>
  <done>src/emit/wireframe/ contains the complete file skeleton (18 emitter stubs + 4 support modules + dispatch + barrel). dispatch.ts switch covers all 18 kinds with never fallback. All stubs throw `NYI: Plan 03-NN` with correct plan tags so downstream executors land in the right follow-up. tsc + biome pass.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — CLI entry stub + package.json script alias</name>
  <files>
    scripts/render-wireframe.ts,
    package.json
  </files>
  <read_first>
    - .planning/phases/03-wireframe-renderer-dogfood-gate/03-RESEARCH.md §Code Examples §CLI entry lines 814-841 (full CLI template verbatim)
    - .planning/phases/03-wireframe-renderer-dogfood-gate/03-PATTERNS.md §scripts/render-wireframe.ts (full template + gotcha list)
    - .planning/phases/03-wireframe-renderer-dogfood-gate/03-CONTEXT.md (Claude's Discretion §CLI form: npx tsx, no bin entry, LF output, exit codes)
    - src/serialize/parse.ts (parseSpecFile signature — returns `{ spec, astHandle, diagnostics, body }`)
    - package.json (existing scripts block — mirror the shape of `test`, `typecheck`, `lint`)
  </read_first>
  <behavior>
    - scripts/render-wireframe.ts is a self-contained CLI entry:
      - argv parsing via `process.argv.slice(2)` — expects `<spec-path> <screen-id>`
      - usage-error prints to stderr and exits 2 if args missing
      - calls `parseSpecFile(specPath)`
      - if `result.spec === null`: print diagnostics to stderr, exit 1
      - otherwise: call `render(result.spec, screenId, { diagnostics: result.diagnostics })` and pipe to stdout
      - top-level catch logs `(err as Error).message` to stderr and exits 1
    - At Wave-0, `render()` throws NYI — the CLI still compiles (imports resolve correctly); actual end-to-end invocation fails with the NYI tag pointing at Plan 03-08. This is the intended intermediate state.
    - package.json gains `"wireframe": "tsx scripts/render-wireframe.ts"` in `scripts` so `npm run wireframe -- <spec> <screen>` works.
  </behavior>
  <action>
    CREATE scripts/render-wireframe.ts (mkdir -p scripts/ first):
    ```typescript
    // scripts/render-wireframe.ts
    // CLI: npx tsx scripts/render-wireframe.ts <spec-path> <screen-id>
    // Emits fixed-60-col ASCII wireframe for the named screen to stdout.
    //
    // Exit codes (RESEARCH Open Q 3 recommendation):
    //   0 — render succeeded (may include [BROKEN LINK] markers on Stage-B errors)
    //   1 — spec is null (Stage-A fatal or missing frontmatter); diagnostics to stderr
    //   2 — usage error (missing argv)
    //
    // `[BROKEN LINK]` markers injected inline when parseSpecFile returns
    // Stage-B error diagnostics; renderer never throws except on unknown screenId.
    import { parseSpecFile } from "../src/serialize/index.ts";
    import { render } from "../src/emit/wireframe/index.ts";

    async function main(): Promise<void> {
      const [specPath, screenId] = process.argv.slice(2);
      if (!specPath || !screenId) {
        process.stderr.write("usage: render-wireframe <spec-path> <screen-id>\n");
        process.exit(2);
      }
      const result = await parseSpecFile(specPath);
      if (!result.spec) {
        process.stderr.write(
          `parse failed:\n${result.diagnostics
            .map((d) => `  ${d.code} @ ${d.path}: ${d.message}`)
            .join("\n")}\n`,
        );
        process.exit(1);
      }
      const out = render(result.spec, screenId, { diagnostics: result.diagnostics });
      process.stdout.write(out);
    }

    main().catch((err) => {
      process.stderr.write(`error: ${(err as Error).message}\n`);
      process.exit(1);
    });
    ```

    EDIT package.json — ADD inside the existing `"scripts": { ... }` object (after `"format"`):
    ```json
    "wireframe": "tsx scripts/render-wireframe.ts"
    ```
    Preserve all existing keys + values byte-identically; only add the new entry. Do NOT add a new dep (tsx is invoked via npx; pinning as a dep is explicitly deferred per RESEARCH §Standard Stack line 141).
  </action>
  <verify>
    <automated>test -f scripts/render-wireframe.ts && grep -q '"wireframe"' package.json && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/render-wireframe.ts` returns 0
    - `grep -q "parseSpecFile" scripts/render-wireframe.ts` returns 0
    - `grep -q "from \"../src/serialize/index.ts\"" scripts/render-wireframe.ts` returns 0
    - `grep -q "from \"../src/emit/wireframe/index.ts\"" scripts/render-wireframe.ts` returns 0
    - `grep -q "process.argv.slice(2)" scripts/render-wireframe.ts` returns 0
    - `grep -qE "process\\.exit\\(2\\)" scripts/render-wireframe.ts` returns 0 (usage-error exit code)
    - `grep -q '"wireframe":' package.json` returns 0
    - `grep -q '"tsx scripts/render-wireframe.ts"' package.json` returns 0
    - `npx tsc --noEmit` exits 0
    - `npx biome check .` exits 0
    - `npx vitest run` exits 0 (no regression on Phase 1/2 suite; baseline 425/425 remains intact)
  </acceptance_criteria>
  <done>scripts/render-wireframe.ts compiles against the Wave-0 stubs. package.json exposes `npm run wireframe -- <spec> <screen>`. No runtime dep added. Full `npx vitest run && npx tsc --noEmit && npx biome check .` gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → parseSpecFile → render | Developer runs `npx tsx scripts/render-wireframe.ts <spec-path> <screen-id>`. argv is trusted developer input (not a server endpoint). Spec-path reads from filesystem. |
| fixtures/wireframes/*.wf.txt → stdout/clipboard (Phase 8 + human paste-into-PR) | Persisted ASCII output crosses into untrusted paste targets (GitHub PR descriptions, Slack, email clients). |
| SHARED.md → dogfood-gate test parser | Human-edited YAML frontmatter consumed by `tests/dogfood-gate.test.ts` via gray-matter + eemeli/yaml. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | `fixtures/wireframes/*.wf.txt` ← smuggled ANSI escape sequences in future label data | accept (Phase-3) | Phase 1 D-03 already restricts labels to printable ASCII at schema time. Phase 3 Plan 03-05/03-06 emitters pass labels through `truncate`/`padRight` — no transform re-introduces control bytes. `tests/wireframe-ascii-baseline.test.ts` regex `/^[|\-+. \x20-\x7E\n]*$/` enforces at output-time (WIREFRAME-02). Wave-0 ships this test RED-protective. |
| T-03-02 | Tampering | `scripts/render-wireframe.ts` reading arbitrary filesystem paths via argv | accept (developer-tool scope) | CLI is a developer tool, not a server endpoint. No network surface. Documented in README for Phase 3 that the CLI trusts its argv. No path-traversal sanitisation added in v1; Phase 8 `:yank wireframe` (canvas-driven) restricts to opened specs. |
| T-03-03 | Denial of service | Pathologically deep component trees crashing `renderNode` via stack overflow | accept | Phase 1 ships a 100-level recursion stress test against `ComponentNodeSchema` (Zod stack-bounded evaluator). Phase 3's dispatcher is a plain switch → child-call → return; Node's default call-stack handles 100+ levels trivially at 60-col width. No separate depth guard added. |
| T-03-04 | Repudiation | Snapshot drift from machine-dependent content (Date, env, random) | mitigate | Wave-0 shapes the interface to enforce purity: `render(spec, screenId, opts?)` takes NO Date/env arg. Plan 03-03/03-08 tests include a determinism-check (`renderX(node, w)` called twice → byte-identical output) per RESEARCH Pitfall 3. |
| T-03-05 | Tampering | `SHARED.md` YAML frontmatter via prototype-pollution vectors (`__proto__` injection) | mitigate | Dogfood-gate test parses SHARED.md via gray-matter + eemeli/yaml stack — inherits Phase 2's three-layer prototype-pollution defense (`partitionTopLevel` + `ADVERSARIAL_KEYS`). The SHARED.md schema only recognises `schema`, `shared[]` with nested `{screen, target, date, verdict, notes}` — any other key is ignored by the test's typed reader. No eval, no prototype lookup. |
</threat_model>

<verification>
Full green-gate at plan close:
```bash
npx vitest run && npx tsc --noEmit && npx biome check .
```
All three exit 0; Phase-1 + Phase-2 baselines intact (425/425 tests prior; expect same count or +1-3 pending-skip entries).
</verification>

<success_criteria>
- src/emit/wireframe/ directory tree complete with 18 emitter stubs + 4 support modules + dispatch + barrel
- dispatch.ts exhaustive switch covers all 18 kinds with `const _exhaustive: never = node` compile-time gate
- scripts/render-wireframe.ts CLI stub compiles against Wave-0 scaffolding
- package.json `wireframe` npm script alias wired
- .gitattributes locks *.wf.txt to LF
- tests/wireframe-ascii-baseline.test.ts + tests/wireframe-catalog.test.ts + tests/dogfood-gate.test.ts created with RED-appropriate skip markers
- fixtures/wireframes/README.md + SHARED.md template seeded
- Zero new runtime dependencies (package.json `dependencies` block byte-identical; only `scripts` object gains the wireframe entry)
- `npx vitest run && npx tsc --noEmit && npx biome check .` green
</success_criteria>

<output>
After completion, create `.planning/phases/03-wireframe-renderer-dogfood-gate/03-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- list of files created + their roles
- confirmation that dispatch.ts switch covers all 18 kinds + never fallback
- confirmation that zero new runtime deps added
- regression stats (vitest pass count before + after)
</output>
