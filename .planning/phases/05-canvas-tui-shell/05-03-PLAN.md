---
phase: 05-canvas-tui-shell
plan: "03"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/canvas/panes/screens-list.ts
  - src/canvas/panes/wireframe-preview.ts
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-05
  - CANVAS-06

must_haves:
  truths:
    - "ScreensListPane renders screen names with > prefix for active, two-space prefix for others"
    - "ScreensListPane renders ⚠ suffix for screens with error diagnostics"
    - "Moving cursor via j/k immediately calls onSelect with the new screenId (D-80)"
    - "WireframePreviewPane calls renderSingleVariant(spec, screenId, 'content') on each render tick"
    - "WireframePreviewPane shows placeholder when no screen selected"
    - "Both panes produce lines with visibleWidth <= pane width (chrome hygiene)"
    - "Neither pane emits raw escape sequences via process.stdout.write"
  artifacts:
    - path: "src/canvas/panes/screens-list.ts"
      provides: "ScreensListPane with SelectList, immediate selection, error markers"
      exports: ["ScreensListPane"]
    - path: "src/canvas/panes/wireframe-preview.ts"
      provides: "WireframePreviewPane read-only content-variant renderer"
      exports: ["WireframePreviewPane"]
  key_links:
    - from: "src/canvas/panes/screens-list.ts"
      to: "@mariozechner/pi-tui SelectList"
      via: "import { SelectList } from '@mariozechner/pi-tui'"
      pattern: "import.*SelectList.*pi-tui"
    - from: "src/canvas/panes/wireframe-preview.ts"
      to: "src/emit/wireframe/index.ts"
      via: "import { renderSingleVariant }"
      pattern: "import.*renderSingleVariant.*emit/wireframe"
---

<objective>
Implement ScreensListPane (using pi-tui SelectList with immediate j/k selection) and WireframePreviewPane (read-only content-variant renderer calling Phase 3's renderSingleVariant). These two panes are parallel to 05-02 — they share no files.

Purpose: Complete the two simpler panes so the root canvas assembly (Wave 4) can wire all three panes together.
Output: 2 fully-implemented pane components with tests passing in canvas-render.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-canvas-tui-shell/05-CONTEXT.md
@.planning/phases/05-canvas-tui-shell/05-RESEARCH.md
@.planning/phases/05-canvas-tui-shell/05-PATTERNS.md
@.planning/phases/05-canvas-tui-shell/05-UI-SPEC.md

<interfaces>
<!-- ScreensListPane MUST use SelectList from pi-tui -->
<!-- SelectList API from RESEARCH.md Pattern 5 -->
```typescript
interface SelectItem { value: string; label: string; description?: string; }

// SelectList constructor
const list = new SelectList(items: SelectItem[], maxVisible: number, theme: SelectListTheme);
list.onSelect = (item: SelectItem) => void;        // fires on Enter
list.onCancel = () => void;                         // fires on Escape
list.onSelectionChange = (item: SelectItem) => void; // fires on j/k navigation (D-80: use this)
list.setFilter(query: string): void;
list.setSelectedIndex(n: number): void;
list.getSelectedItem(): SelectItem | null;
list.setItems(items: SelectItem[]): void;
// Renders with built-in scroll; pass handleInput(data) to delegate keyboard
list.handleInput(data: string): void;
list.render(width: number): string[];
list.invalidate(): void;
```

<!-- WireframePreviewPane calls Phase 3 function -->
```typescript
// From src/emit/wireframe/index.ts (Phase 3):
import { renderSingleVariant } from "../../emit/wireframe/index.ts";
// renderSingleVariant(spec: Spec, screenId: string, variant: VariantKind, opts?): string
// Returns multi-line string; split on "\n" to get string[]
// D-73: always pass "content" as variant in Phase 5
```

<!-- Snapshot shape (canvas panes receive this from root) -->
```typescript
export interface Snapshot {
  spec: Spec;
  diagnostics: Diagnostic[];
  dirty: boolean;
  lastWriteResult?: WriteResult;
}
```

<!-- Screen from model (screens-list reads this) -->
// spec.screens: Screen[]
// screen.id: ScreenId (branded string)
// screen.title: string

<!-- Diagnostic shape (for error marker detection) -->
// diagnostic.severity: "error" | "warning" | "info"
// diagnostic.path: string (JSON pointer — contains screen id for screen-level errors)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ScreensListPane with SelectList and immediate selection</name>
  <files>
    src/canvas/panes/screens-list.ts
  </files>
  <read_first>
    - src/canvas/panes/screens-list.ts (existing stub — replace NYI)
    - .planning/phases/05-canvas-tui-shell/05-RESEARCH.md §Pattern 5 SelectList API, §Pitfall 1 line-width
    - .planning/phases/05-canvas-tui-shell/05-PATTERNS.md §screens-list.ts (full code pattern)
    - .planning/phases/05-canvas-tui-shell/05-CONTEXT.md §D-80, D-81, D-82
    - .planning/phases/05-canvas-tui-shell/05-UI-SPEC.md §Copywriting Contract (screens-list copy)
    - tests/canvas-render.test.ts (screens-list rendering tests — implement to GREEN)
    - src/model/screen.ts (Screen type shape)
  </read_first>
  <behavior>
    - Constructor: `ScreensListPane(onSelect: (screenId: string) => void, theme: CanvasTheme)` where `CanvasTheme = { fg: (token: string, str: string) => string }`
    - Has `update(snapshot: Snapshot): void` method — rebuilds SelectList items from `snapshot.spec.screens` in order (D-82)
    - `update()` checks diagnostics for `severity === "error"` per screen id; appends ` ⚠` (space+warning-glyph) to label if error found
    - Uses `onSelectionChange` callback (NOT `onSelect`) for immediate j/k selection per D-80
    - SelectList items: `{ value: screen.id, label: hasError ? screen.title + " ⚠" : screen.title }`
    - `render(width)`: returns `this.list.render(width).map(line => truncateToWidth(line, width))`
    - `handleInput(data)`: delegates to `this.list.handleInput(data)`
    - `invalidate()`: calls `this.list.invalidate()`
    - SelectListTheme uses `theme.fg("accent", t)` for `selectedPrefix` and `selectedText`
    - Item label truncated to pane width minus 4 chars (for `> ` prefix + ⚠ slot) per D-81

    Test coverage to add in canvas-render.test.ts:
    - Given snapshot with 3 screens and no errors: render(30) produces 3 list items, none with ⚠
    - Given snapshot with screen having error diagnostic: its label has ` ⚠` suffix
    - All render(30) lines have visibleWidth <= 30
  </behavior>
  <action>
    Replace stub with full implementation.

    Import `SelectList, truncateToWidth, visibleWidth` from `"@mariozechner/pi-tui"`.
    Import `type Snapshot` from `"../../editor/types.ts"`.

    Key implementation notes:
    - Do NOT pre-bake theme colors in constructor (Pitfall 4) — pass to SelectList theme callbacks which are called at render time
    - The SelectList `theme.selectedPrefix` callback receives the prefix text and returns styled version — use `(t) => this.theme.fg("accent", t)`
    - `this.list.setItems(items)` in `update()` preserves selection if the selected id is still in the new list

    After implementing, add concrete test cases to canvas-render.test.ts for screens-list behavior (see behavior section). Go RED → GREEN → biome check.

    Commit pattern: `test(05-03): RED — ScreensListPane` then `feat(05-03): GREEN — ScreensListPane`.
  </action>
  <verify>
    <automated>cd /Users/rayliu/dev/mobile-tui && npx vitest run tests/canvas-render.test.ts 2>&1 | grep -E "pass|fail|error" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "onSelectionChange" src/canvas/panes/screens-list.ts` exits 0 (immediate selection per D-80)
    - `grep "theme.fg.*accent" src/canvas/panes/screens-list.ts` exits 0 (accent for active item)
    - `grep "truncateToWidth" src/canvas/panes/screens-list.ts` exits 0 (line-width safety)
    - `grep "⚠" src/canvas/panes/screens-list.ts` exits 0 (error marker)
    - `grep "throw.*NYI\|NYI" src/canvas/panes/screens-list.ts` exits non-zero (no stubs remain)
    - `npx vitest run tests/canvas-render.test.ts` — ScreensListPane tests green (≥3 it() passing)
    - `npx tsc --noEmit` exits 0
    - `npx biome check src/canvas/panes/screens-list.ts` exits 0
  </acceptance_criteria>
  <done>ScreensListPane renders screens correctly, propagates immediate selection, marks error screens with ⚠. Tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement WireframePreviewPane — read-only content-variant renderer</name>
  <files>
    src/canvas/panes/wireframe-preview.ts
  </files>
  <read_first>
    - src/canvas/panes/wireframe-preview.ts (existing stub — replace NYI)
    - .planning/phases/05-canvas-tui-shell/05-CONTEXT.md §D-73 (always content variant, read-only)
    - .planning/phases/05-canvas-tui-shell/05-PATTERNS.md §wireframe-preview.ts (full code pattern)
    - .planning/phases/05-canvas-tui-shell/05-RESEARCH.md §Pitfall 1 line-width, §Don't Hand-Roll
    - src/emit/wireframe/index.ts (renderSingleVariant function signature)
    - scripts/render-wireframe.ts (analog: how renderSingleVariant is called)
    - tests/canvas-render.test.ts (wireframe preview test — add and go GREEN)
    - tests/canvas-chrome.test.ts (chrome hygiene gate — must stay GREEN)
  </read_first>
  <behavior>
    - Constructor: `WireframePreviewPane()` (no args; theme not needed — wireframe is already ASCII-styled)
    - Has `update(snapshot: Snapshot, screenId: string): void` — stores snapshot + screenId, clears line cache
    - `render(width: number)`:
      - If `!this.snapshot || !this.activeScreenId`: return `[truncateToWidth("(no screen selected)", width)]`
      - Otherwise: call `renderSingleVariant(this.snapshot.spec, this.activeScreenId, "content", { diagnostics: this.snapshot.diagnostics })`
      - Split result on `"\n"`, map each line through `truncateToWidth(line, width)` (Pitfall 1 guard)
      - Cache result as `this.lineCache` — invalidate on `update()`
    - `invalidate()`: sets `this.lineCache = null`
    - `handleInput()`: intentionally empty (read-only in Phase 5, per D-73)
    - CRITICAL: The `content` variant is ALWAYS used (D-73) — no variant state tracked in Phase 5

    Pane-width note: WireframePreviewPane renders at the pane width passed to render(). The Phase 3 renderer uses PHONE_WIDTH=60 internally — if pane width < 60, lines will be truncated by truncateToWidth (correct behavior, not a bug).

    Test coverage to add:
    - Given a snapshot with the habit-tracker fixture and a valid screenId, `render(40)` produces non-empty string array where all lines have visibleWidth <= 40
    - `render(40)` with no snapshot → `["(no screen selected)"]` or similar placeholder, length 1
    - `render(40).join('\n')` contains no `\x1b[?` sequences (chrome gate — verify still GREEN)
  </behavior>
  <action>
    Replace stub with the full WireframePreviewPane implementation.

    Import `renderSingleVariant` from `"../../emit/wireframe/index.ts"`.
    Import `truncateToWidth` from `"@mariozechner/pi-tui"`.
    Import `type Snapshot` from `"../../editor/types.ts"`.

    The implementation is primarily a cache-invalidating wrapper around `renderSingleVariant`. Keep it under 60 LOC.

    IMPORTANT: Never call `process.stdout.write` — output only through `render()` return value.

    Add the tests to canvas-render.test.ts and canvas-chrome.test.ts. For the integration test in canvas-render.test.ts that uses a real fixture, use `parseSpecFile("fixtures/habit-tracker.spec.md")` directly in the test (wrap in `beforeAll` or inline `await`).

    Commit pattern: `test(05-03): RED — WireframePreviewPane` then `feat(05-03): GREEN — WireframePreviewPane`.
  </action>
  <verify>
    <automated>cd /Users/rayliu/dev/mobile-tui && npx vitest run tests/canvas-render.test.ts tests/canvas-chrome.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "renderSingleVariant" src/canvas/panes/wireframe-preview.ts` exits 0 (Phase 3 function used)
    - `grep '"content"' src/canvas/panes/wireframe-preview.ts` exits 0 (D-73: always content variant)
    - `grep "truncateToWidth" src/canvas/panes/wireframe-preview.ts` exits 0 (line-width safety)
    - `grep "lineCache" src/canvas/panes/wireframe-preview.ts` exits 0 (caching pattern)
    - `grep "process.stdout" src/canvas/panes/wireframe-preview.ts` exits non-zero (no raw writes)
    - `npx vitest run tests/canvas-render.test.ts` — WireframePreviewPane tests green (≥2 it() passing)
    - `npx vitest run tests/canvas-chrome.test.ts` exits 0 (chrome gate still GREEN after pane added)
    - `npx tsc --noEmit` exits 0
    - `npx biome check src/canvas/panes/wireframe-preview.ts` exits 0
  </acceptance_criteria>
  <done>WireframePreviewPane renders content variant of selected screen. Empty state placeholder shown when no screen selected. Chrome gate GREEN. Tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec content → render output | Screen title and wireframe content from user-edited spec file flows into TUI output |
| Diagnostic path matching | Screen error detection uses diagnostic.path.includes(screen.id) — substring match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | Screen title with ANSI codes injected from spec → escapes in render | mitigate | truncateToWidth strips oversized content; spec validated by validateSpec before store accepts it |
| T-05-08 | Information Disclosure | Wireframe render error exposes internal stack trace in TUI output | mitigate | Wrap renderSingleVariant call in try/catch; return error placeholder string on throw |
| T-05-09 | Tampering | Escape sequence injection via screen title rendered in SelectList | mitigate | CANVAS-06 chrome test: root.render(80).join('\n') must not match \x1b[? — gates this regression |
| T-05-10 | Denial of Service | renderSingleVariant called for non-existent screenId (deleted screen) | mitigate | Root canvas auto-selects first screen if activeScreenId missing from spec.screens on subscribe tick |
</threat_model>

<verification>
```bash
cd /Users/rayliu/dev/mobile-tui
npx vitest run tests/canvas-render.test.ts tests/canvas-chrome.test.ts
npx tsc --noEmit
npx biome check src/canvas/panes/
```
</verification>

<success_criteria>
- ScreensListPane renders correct list items with accent prefix and ⚠ error markers
- WireframePreviewPane shows content variant from renderSingleVariant; placeholder when empty
- All render lines within pane width (no pi-tui throws)
- Chrome test remains GREEN (no escape sequences)
- npx tsc --noEmit and biome check exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-canvas-tui-shell/05-03-SUMMARY.md`
</output>
