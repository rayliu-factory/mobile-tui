---
phase: 04-editor-store-commands-undo
plan: 05
type: tdd
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - src/editor/commands/add-entity.ts
  - src/editor/commands/add-entity.test.ts
  - src/editor/commands/rename-entity.ts
  - src/editor/commands/rename-entity.test.ts
  - src/editor/commands/delete-entity.ts
  - src/editor/commands/delete-entity.test.ts
  - src/editor/commands/add-field.ts
  - src/editor/commands/add-field.test.ts
  - src/editor/commands/rename-field.ts
  - src/editor/commands/rename-field.test.ts
  - src/editor/commands/delete-field.ts
  - src/editor/commands/delete-field.test.ts
  - src/editor/commands/set-field-type.ts
  - src/editor/commands/set-field-type.test.ts
  - src/editor/commands/add-relationship.ts
  - src/editor/commands/add-relationship.test.ts
  - src/editor/commands/delete-relationship.ts
  - src/editor/commands/delete-relationship.test.ts
  - src/editor/commands/add-action.ts
  - src/editor/commands/add-action.test.ts
  - src/editor/commands/rename-action.ts
  - src/editor/commands/rename-action.test.ts
  - src/editor/commands/delete-action.ts
  - src/editor/commands/delete-action.test.ts
  - src/editor/commands/set-action-effect.ts
  - src/editor/commands/set-action-effect.test.ts
  - src/editor/commands/set-nav-root.ts
  - src/editor/commands/set-nav-root.test.ts
  - src/editor/commands/add-nav-edge.ts
  - src/editor/commands/add-nav-edge.test.ts
  - src/editor/commands/update-nav-edge.ts
  - src/editor/commands/update-nav-edge.test.ts
  - src/editor/commands/delete-nav-edge.ts
  - src/editor/commands/delete-nav-edge.test.ts
  - src/editor/commands/set-tabbar-items.ts
  - src/editor/commands/set-tabbar-items.test.ts
autonomous: true
requirements:
  - EDITOR-02

must_haves:
  truths:
    - "add-entity appends to spec.dataModel.entities AND doc.addIn to AST"
    - "rename-entity cascades through Field.of references, Action submit.entity, and Relationship from/to"
    - "add-action creates the registry entry per D-59 two-command split"
    - "rename-action cascades through all ComponentNode.action bindings and NavEdge.trigger"
    - "add-nav-edge appends to navigation.edges (MVP D-55)"
    - "Every command: apply→invert→apply idempotence verified with ≥3 fixtures"
    - "Total command count after this plan: 34 commands (exhaustive catalog, D-54)"
  artifacts:
    - path: "src/editor/commands/add-entity.ts"
      provides: "MVP add-entity (D-55)"
      exports: ["addEntity"]
    - path: "src/editor/commands/add-field.ts"
      provides: "MVP add-field (D-55)"
      exports: ["addField"]
    - path: "src/editor/commands/add-nav-edge.ts"
      provides: "MVP add-nav-edge (D-55)"
      exports: ["addNavEdge"]
    - path: "src/editor/commands/add-action.ts"
      provides: "D-59 action registry creation"
      exports: ["addAction"]
    - path: "src/editor/commands/rename-action.ts"
      provides: "D-59 cascade through component bindings + NavEdge.trigger"
      exports: ["renameAction"]
  key_links:
    - from: "src/editor/commands/rename-entity.ts"
      to: "src/model/data.ts"
      via: "Relationship.from/to cascade on entity rename"
      pattern: "Relationship"
    - from: "src/editor/commands/rename-action.ts"
      to: "src/model/cross-reference.ts"
      via: "walkComponentTree reused for cascade through component action bindings"
      pattern: "walkComponentTree"
---

<objective>
Implement the remaining 18 commands to complete the exhaustive catalog (D-54): 9 data-model commands (add/rename/delete-entity, add/rename/delete-field, set-field-type, add/delete-relationship), 4 action-registry commands (add/rename/delete-action, set-action-effect), 4 navigation commands (set-nav-root, add/update/delete-nav-edge), and 1 TabBar command (set-tabbar-items). After this plan, the full 34-command catalog is complete and the registry barrel can be wired.

Purpose: Exhaustive command catalog (D-54) enables Phase 5/6 to have zero command gaps. Every mutable Spec leaf has a named command.

Output: 18 command files + 18 test files under src/editor/commands/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-editor-store-commands-undo/04-CONTEXT.md
@.planning/phases/04-editor-store-commands-undo/04-RESEARCH.md
@.planning/phases/04-editor-store-commands-undo/04-PATTERNS.md
@.planning/phases/04-editor-store-commands-undo/04-03-SUMMARY.md
@.planning/phases/04-editor-store-commands-undo/04-04-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. -->

From src/model/data.ts (data model shapes):
```typescript
// Entity: { name: string; fields: Field[]; relationships?: Relationship[] }
// Field: { name: string; type: FieldType; of?: string; required?: boolean }
// FieldType: "string" | "number" | "boolean" | "date" | "reference" | "enum"
// Relationship: { from: string; to: string; kind: "has_one" | "has_many" | "belongs_to" }
// DataModel: { entities: Entity[] }
```

From src/model/action.ts (action shapes):
```typescript
// Action = { kind: "navigate"; screen: ScreenId }
//         | { kind: "present"; overlay: ScreenId }
//         | { kind: "dismiss" }
//         | { kind: "submit"; entity: string; fields: string[] }
//         | { kind: "custom"; handler: string }
//         | { kind: "external"; url: string }
// spec.actions: Record<ActionId, Action>
```

From src/model/navigation.ts:
```typescript
// NavEdge: { from: ScreenId; to: ScreenId; trigger: ActionId; transition?: string }
// NavigationGraph: { root: ScreenId; edges: NavEdge[] }
```

From src/model/component.ts — TabBar-specific:
```typescript
// TabBar component has: items: TabItem[]
// TabItem: { label: string; screenId: ScreenId; action?: ActionId }
```

From src/model/cross-reference.ts:
```typescript
export function walkComponentTree(nodes: ComponentNode[], path: ReadonlyArray<string|number>, ctx: WalkContext): void
// ctx.declaredActions is used for action ref checks — reuse the walk to find all action bindings
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Data model commands — entities, fields, relationships (RED → GREEN)</name>
  <files>
    src/editor/commands/add-entity.ts, src/editor/commands/add-entity.test.ts,
    src/editor/commands/rename-entity.ts, src/editor/commands/rename-entity.test.ts,
    src/editor/commands/delete-entity.ts, src/editor/commands/delete-entity.test.ts,
    src/editor/commands/add-field.ts, src/editor/commands/add-field.test.ts,
    src/editor/commands/rename-field.ts, src/editor/commands/rename-field.test.ts,
    src/editor/commands/delete-field.ts, src/editor/commands/delete-field.test.ts,
    src/editor/commands/set-field-type.ts, src/editor/commands/set-field-type.test.ts,
    src/editor/commands/add-relationship.ts, src/editor/commands/add-relationship.test.ts,
    src/editor/commands/delete-relationship.ts, src/editor/commands/delete-relationship.test.ts
  </files>

  <read_first>
    - src/model/data.ts — Entity, Field, Relationship, DataModel, FieldType shapes
    - src/model/action.ts — Action discriminated union (submit.entity field for cascade)
    - fixtures/habit-tracker.spec.md — has dataModel with entities for cascade tests
    - 04-RESEARCH.md §8 commands 21-29 — exact argsSchema + cascade descriptions
  </read_first>

  <behavior>
    add-entity (MVP, D-55):
    - argsSchema: z.object({ name: z.string().min(1), fields: z.array(FieldSchema).optional().default([]) })
    - apply: append Entity; doc.addIn(["dataModel", "entities"], doc.createNode({name, fields: []})); inverseArgs = { insertedIndex: entities.length }
    - invert: remove by index

    rename-entity:
    - argsSchema: z.object({ from: z.string().min(1), to: z.string().min(1) })
    - apply cascade: update entities[i].name; update every Field.of that === from; update every Action kind==="submit" where action.entity===from; update every Relationship.from/to === from; inverseArgs = { prevName: from, entityIndex, cascadedFieldPaths, cascadedActionIds, cascadedRelationships }
    - invert: reverse all cascade ops

    delete-entity:
    - argsSchema: z.object({ name: z.string().min(1) })
    - apply: capture entity JSON; remove; remove relationships where from===name OR to===name; emit EDITOR_REF_CASCADE_INCOMPLETE for orphan field/action refs; inverseArgs = { entityJSON, entityIndex, removedRelationships }
    - invert: restore entity + relationships

    add-field (MVP, D-55):
    - argsSchema: z.object({ entity: z.string().min(1), field: FieldSchema })
    - apply: find entity; push field; doc.addIn([..entity path.., "fields"], doc.createNode(field)); inverseArgs = { entity, insertedFieldIndex: fields.length }
    - invert: delete field at insertedFieldIndex

    rename-field:
    - argsSchema: z.object({ entity: z.string().min(1), from: z.string().min(1), to: z.string().min(1) })
    - apply: update field name; cascade JSON-Pointer bindings in components (if any reference /entity/from → /entity/to); inverseArgs = { entity, fieldIndex, prevName: from }
    - invert: rename back

    delete-field:
    - argsSchema: z.object({ entity: z.string().min(1), name: z.string().min(1) })
    - apply: capture field JSON; remove; inverseArgs = { entity, fieldJSON, fieldIndex }
    - invert: restore at fieldIndex

    set-field-type:
    - argsSchema: z.object({ entity: z.string().min(1), fieldName: z.string().min(1), type: z.enum(["string","number","boolean","date","reference","enum"]), of: z.string().optional() })
    - apply: scalar edit on fields[i].type + fields[i].of; setScalarPreserving for scalar edits; inverseArgs = { entity, fieldIndex, prevType, prevOf }
    - invert: restore prevType + prevOf

    add-relationship:
    - argsSchema: z.object({ entity: z.string().min(1), from: z.string().min(1), to: z.string().min(1), kind: z.enum(["has_one","has_many","belongs_to"]) })
    - apply: find entity; push relationship; doc.addIn([..entity path.., "relationships"], doc.createNode({from, to, kind})); inverseArgs = { entity, insertedRelIndex }
    - invert: doc.deleteIn at insertedRelIndex

    delete-relationship:
    - argsSchema: z.object({ entity: z.string().min(1), index: z.coerce.number().int().min(0) })
    - apply: capture relationship JSON; remove at index; inverseArgs = { entity, index, relJSON }
    - invert: restore at index

    ≥3 fixtures per command across habit-tracker, todo, social-feed fixtures.
  </behavior>

  <action>
    Write 9 command files + 9 test files. All follow the per-command template from 04-PATTERNS.md. For cascade commands (rename-entity), walk spec.actions via Object.entries and check each action's discriminated kind. For cascade in field types and relationships, iterate spec.dataModel.entities. AST paths for entities follow: ["dataModel", "entities", entityIndex, fieldName, ...].

    TDD commit: `test(04-05a): RED — data model commands` → `feat(04-05a): GREEN — entity/field/relationship commands`
  </action>

  <verify>
    <automated>npx vitest run src/editor/commands/add-entity.test.ts src/editor/commands/rename-entity.test.ts src/editor/commands/delete-entity.test.ts src/editor/commands/add-field.test.ts src/editor/commands/rename-field.test.ts src/editor/commands/delete-field.test.ts src/editor/commands/set-field-type.test.ts src/editor/commands/add-relationship.test.ts src/editor/commands/delete-relationship.test.ts</automated>
  </verify>

  <done>All 9 data model command test files pass with ≥3 idempotence fixtures each; npx tsc --noEmit exits 0</done>
</task>

<task type="tdd">
  <name>Task 2: Action registry + navigation + TabBar commands (RED → GREEN)</name>
  <files>
    src/editor/commands/add-action.ts, src/editor/commands/add-action.test.ts,
    src/editor/commands/rename-action.ts, src/editor/commands/rename-action.test.ts,
    src/editor/commands/delete-action.ts, src/editor/commands/delete-action.test.ts,
    src/editor/commands/set-action-effect.ts, src/editor/commands/set-action-effect.test.ts,
    src/editor/commands/set-nav-root.ts, src/editor/commands/set-nav-root.test.ts,
    src/editor/commands/add-nav-edge.ts, src/editor/commands/add-nav-edge.test.ts,
    src/editor/commands/update-nav-edge.ts, src/editor/commands/update-nav-edge.test.ts,
    src/editor/commands/delete-nav-edge.ts, src/editor/commands/delete-nav-edge.test.ts,
    src/editor/commands/set-tabbar-items.ts, src/editor/commands/set-tabbar-items.test.ts
  </files>

  <read_first>
    - src/model/action.ts — Action discriminated union + ActionSchema
    - src/model/navigation.ts — NavEdge, NavigationGraph
    - src/model/component.ts — TabBar items shape (TabItem with label, screenId, action?)
    - src/model/cross-reference.ts — walkComponentTree for action cascade
    - 04-RESEARCH.md §8 commands 17-34 — exact spec descriptions for each command
    - fixtures/habit-tracker.spec.md — has actions + nav edges for cascade tests
  </read_first>

  <behavior>
    add-action (D-59):
    - argsSchema: z.object({ id: ActionIdSchema, effect: ActionSchema }) where ActionSchema is the full discriminated union from model
    - apply: `{ ...spec, actions: { ...spec.actions, [id]: effect } }`; doc.setIn(["actions", id], doc.createNode(effect)); inverseArgs = { id }
    - invert: delete id from spec.actions; doc.deleteIn(["actions", id])

    rename-action (D-59 cascade):
    - argsSchema: z.object({ from: ActionIdSchema, to: ActionIdSchema })
    - apply cascade: copy spec.actions[from] to spec.actions[to]; delete spec.actions[from]; walk EVERY screen's EVERY variant's component tree via walkComponentTree — for each node with node.action===from, update to `to`; walk NavEdge.trigger === from, update to `to`; AST: doc.setIn + doc.deleteIn for registry; setScalarPreserving for each component binding; inverseArgs = { prevName: from, cascadedComponentPaths, cascadedNavEdgeIndices }
    - invert: reverse rename

    delete-action:
    - argsSchema: z.object({ id: ActionIdSchema })
    - apply: capture action JSON; delete; emit EDITOR_REF_CASCADE_INCOMPLETE for any component still binding to `id`; inverseArgs = { id, actionJSON }
    - invert: restore action

    set-action-effect:
    - argsSchema: z.object({ id: ActionIdSchema, effect: ActionSchema })
    - apply: replace effect; doc.setIn(["actions", id], doc.createNode(effect)); inverseArgs = { id, prevEffect: spec.actions[id].toJSON() } — note: spec.actions[id] is already JS, just clone via JSON spread
    - invert: restore prevEffect

    set-nav-root:
    - argsSchema: z.object({ screenId: ScreenIdSchema })
    - apply: setScalarPreserving(doc, ["navigation", "root"], screenId); inverseArgs = { prevRoot: spec.navigation.root }
    - invert: restore prevRoot

    add-nav-edge (MVP, D-55):
    - argsSchema: z.object({ from: ScreenIdSchema, to: ScreenIdSchema, trigger: ActionIdSchema, transition: z.string().optional() })
    - apply: append NavEdge; doc.addIn(["navigation", "edges"], doc.createNode({from, to, trigger})); inverseArgs = { insertedIndex: spec.navigation.edges.length }
    - invert: remove by index

    update-nav-edge:
    - argsSchema: z.object({ index: z.coerce.number().int().min(0), patch: z.object({ from: ScreenIdSchema.optional(), to: ScreenIdSchema.optional(), trigger: ActionIdSchema.optional(), transition: z.string().optional() }) })
    - apply: merge patch into edges[index]; setScalarPreserving for each changed field; inverseArgs = { index, prevEdge: { ...edge } }
    - invert: restore prevEdge fields

    delete-nav-edge:
    - argsSchema: z.object({ index: z.coerce.number().int().min(0) })
    - apply: capture edge JSON; remove; doc.deleteIn(["navigation", "edges", index]); inverseArgs = { index, edgeJSON }
    - invert: restore at index via doc.setIn

    set-tabbar-items:
    - argsSchema: z.object({ screenId: ScreenIdSchema, variantKind: z.enum(["content","empty","loading","error"]), path: JsonPointerSchema, items: z.array(TabItemSchema) })
    - apply: find TabBar at path in variant tree; update items; doc.setIn([...tabBarAstPath, "items"], doc.createNode(items)); inverseArgs = { tabBarAstPath, prevItems }
    - invert: restore prevItems via doc.setIn

    ≥3 fixtures per command — use habit-tracker (has actions + nav), todo (simpler), social-feed (has many screens + nav edges).
  </behavior>

  <action>
    Write 9 command files + 9 test files. For rename-action, use walkComponentTree from src/model/cross-reference.ts — but note WalkContext.declaredActions is a Set; for our cascade we need a different context. Create a local `walkForActionRefs(spec, fromActionId): Array<{ screenIndex, variantKind, astPath: (string|number)[] }>` helper in rename-action.ts that walks all screens × variants × tree and collects all nodes where node.action === fromActionId. Then setScalarPreserving each.

    For delete-action, similar walk to emit EDITOR_REF_CASCADE_INCOMPLETE info diagnostics for each orphan binding — these are informational, not errors.

    TDD commit: `test(04-05b): RED — action/nav/tabbar commands` → `feat(04-05b): GREEN — action registry + navigation + tabbar commands`
  </action>

  <verify>
    <automated>npx vitest run src/editor/commands/add-action.test.ts src/editor/commands/rename-action.test.ts src/editor/commands/delete-action.test.ts src/editor/commands/set-action-effect.test.ts src/editor/commands/set-nav-root.test.ts src/editor/commands/add-nav-edge.test.ts src/editor/commands/update-nav-edge.test.ts src/editor/commands/delete-nav-edge.test.ts src/editor/commands/set-tabbar-items.test.ts</automated>
  </verify>

  <done>All 9 command test files pass with ≥3 idempotence fixtures each; total command count is now 34; npx tsc --noEmit exits 0; npx biome check . exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rename-action cascade | Walks all screens × variants × component trees; must not mutate AST nodes it doesn't intend to change |
| delete-action orphan refs | Leaves component bindings pointing to non-existent action; validateSpec post-apply surfaces these as errors blocking save |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Tampering | rename-action with collision (to already exists) | mitigate | validateSpec post-apply catches duplicate action ids in registry |
| T-04-17 | Tampering | add-nav-edge with non-existent from/to screen ids | mitigate | validateSpec post-apply catches dangling NavEdge refs; save-gate blocks write |
| T-04-18 | Tampering | delete-entity with orphan field bindings in component props | mitigate | EDITOR_REF_CASCADE_INCOMPLETE info diagnostic emitted; validateSpec catches structural breaks; save gated |
| T-04-19 | DoS | rename-action cascade walk on 50-screen spec with deep trees | accept | walkForActionRefs is O(N-nodes); for typical specs (<20 screens, <50 nodes each) < 1ms; documented in command header |
</threat_model>

<verification>
- `npx vitest run src/editor/commands/` — all 34 command test files pass (18 from this plan + 16 from prior plans)
- `npx tsc --noEmit` — 0 errors
- `npx biome check .` — 0 errors
- `ls src/editor/commands/*.ts | grep -v test | wc -l` — 34 + _path-utils.ts = 35 files
- rename-action cascade test: rename an action that appears in 2+ component bindings → both bindings updated in the returned spec
</verification>

<success_criteria>
1. 34 command files total in src/editor/commands/ (18 from this plan, completing the exhaustive catalog D-54)
2. rename-action cascades through all component tree bindings and NavEdge.trigger
3. rename-entity cascades through Field.of, Action.submit.entity, Relationship.from/to
4. add-nav-edge is the MVP last item from D-55 (all 11 MVP commands now complete)
5. All 34 commands have co-located test files with ≥3 idempotence fixtures each
6. All existing Phase 1/2/3 tests still pass (regression)
</success_criteria>

<output>
After completion, create `.planning/phases/04-editor-store-commands-undo/04-05-SUMMARY.md`
</output>
