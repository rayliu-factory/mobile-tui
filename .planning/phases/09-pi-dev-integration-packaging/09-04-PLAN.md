---
phase: 09-pi-dev-integration-packaging
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - README.md
  - .planning/phases/09-pi-dev-integration-packaging/09-HUMAN-UAT.md
autonomous: false
requirements:
  - PI-07
  - PI-08

must_haves:
  truths:
    - "README.md covers all sections listed in D-311: install, /spec flow, wizard steps, canvas keybindings, handoff commands, YAML schema example"
    - "README reader with no prior context can produce a working spec following the README alone"
    - "HUMAN-UAT.md has a completed PI-07 author certification block (two pi version certs)"
    - "tests/no-pi-bundle.test.ts passes after npm run build"
  artifacts:
    - path: "README.md"
      provides: "Complete user-facing documentation per PI-08 + D-311"
      contains: "pi install npm:mobile-tui"
    - path: ".planning/phases/09-pi-dev-integration-packaging/09-HUMAN-UAT.md"
      provides: "PI-07 manual certification record"
  key_links:
    - from: "README.md §Install"
      to: "package.json name: mobile-tui"
      via: "pi install npm:mobile-tui command"
      pattern: "mobile-tui"
    - from: "README.md §Wizard Steps"
      to: "STEP_DEFINITIONS in src/wizard/steps/index.ts"
      via: "matches 8 linear steps"
      pattern: "8 steps"
---

<objective>
Write the README (PI-08) and complete the manual two-version certification gate (PI-07). The README must let a dev with no prior context produce a working spec following the README alone. The PI-07 cert documents the author running `/spec` end-to-end on two pi versions.

Purpose: Final delivery gate. README and manual cert are the last two requirements before the phase is complete.

Output: README.md, completed 09-HUMAN-UAT.md PI-07 certification block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-pi-dev-integration-packaging/09-CONTEXT.md
@.planning/phases/09-pi-dev-integration-packaging/09-RESEARCH.md
@.planning/phases/09-pi-dev-integration-packaging/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <read_first>
    - .planning/phases/09-pi-dev-integration-packaging/09-CONTEXT.md D-311 (exact README content requirements)
    - src/wizard/steps/index.ts (to get the exact 8 step names for the wizard section)
    - src/canvas/root.ts (to verify keybinding list for canvas section — Ctrl+P, Ctrl+Z, Ctrl+Q, Tab)
    - src/editor/commands/ directory (check which handoff commands exist: yank-wireframe, prompt-screen, extract-screen)
    - fixtures/habit-tracker.spec.md or any fixture file (for the YAML schema example — copy a real frontmatter block)
    - .planning/REQUIREMENTS.md §PI-01..PI-08 (cross-check nothing is hidden)
  </read_first>
  <files>README.md</files>
  <action>
Create `README.md` at the repo root. It MUST contain all sections listed in D-311:

**Required sections (D-311):**
1. **Install** — `pi install npm:mobile-tui`
2. **`/spec` flow** — both the fresh-project wizard path AND the existing-spec canvas path
3. **Wizard step overview** — all 8 steps by name (read src/wizard/steps/index.ts for authoritative step names)
4. **Canvas keybindings** — global shortcuts: `Ctrl+P` (command palette), `Ctrl+Z` (undo), `Ctrl+Q` (quit), `Tab`/`Shift+Tab` (focus cycle)
5. **Handoff commands** — `:yank wireframe <screen-id>`, `:prompt screen <id> <target>`, `:extract --screen <id>` (read src/editor/commands/ to verify these exist)
6. **Annotated YAML frontmatter block** — a real spec schema example showing the core shape (copy from a fixture and annotate)

**Additional recommended sections:**
- **Overview** — one-paragraph description of what mobile-tui does (from PROJECT.md core value)
- **Requirements** — pi.dev v0.67.x or later; Node.js 20+
- **The spec file format** — brief explanation of Markdown + YAML frontmatter + ASCII wireframes

**Quality bar per D-311:** A developer with no prior context can produce a working spec from the README instructions alone. This means:
- Installation command is copy-paste runnable
- `/spec` invocation is shown with both paths
- Wizard step names are listed so the dev knows what to expect
- At least one annotated spec snippet shows the YAML shape
- Handoff commands are explained with target options (swiftui / compose / tests)

**Tone:** Concise, developer-facing, no marketing language. Code blocks for all commands and YAML.

After writing, verify:
- `grep "pi install npm:mobile-tui" README.md` exits 0
- `grep "/spec" README.md` exits 0
- `grep "yank wireframe" README.md` exits 0
- File has ≥ 60 lines (substantive, not a stub)
  </action>
  <verify>
    <automated>grep "pi install npm:mobile-tui" README.md && grep "/spec" README.md && grep "yank wireframe" README.md && wc -l README.md | awk '{print ($1 >= 60) ? "OK" : "TOO_SHORT"}'</automated>
  </verify>
  <done>
    - README.md exists with all 6 required D-311 sections
    - Contains `pi install npm:mobile-tui` install command
    - Documents both wizard path (fresh project) and canvas path (existing SPEC.md)
    - Lists all 8 wizard steps by name
    - Documents canvas global keybindings (Ctrl+P, Ctrl+Z, Ctrl+Q, Tab)
    - Documents all three handoff commands with target options
    - Contains annotated YAML frontmatter block showing spec shape
    - README is ≥ 60 lines (substantive content)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PI-07 Manual Certification Gate (two pi versions)</name>
  <read_first>
    - .planning/phases/09-pi-dev-integration-packaging/09-HUMAN-UAT.md (existing UAT file — add cert block here)
    - .planning/phases/09-pi-dev-integration-packaging/09-RESEARCH.md §Environment Availability
    - .planning/phases/09-pi-dev-integration-packaging/09-CONTEXT.md D-310
  </read_first>
  <what-built>
The full Phase 9 integration is complete:
- tsup config + package.json updated (Plan 01)
- src/session.ts with security controls (Plan 02)
- src/extension.ts wiring pi runtime (Plan 03)
- TUI stubs replaced with real pi-tui types (Plan 03)
- npm run build produces dist/ (Plan 03)

Before manual cert: run the bundle invariant test to confirm pi-* packages are not bundled:
```bash
npm run build && npx vitest run tests/no-pi-bundle.test.ts
```
This must pass before proceeding.
  </what-built>
  <how-to-verify>
**Prerequisite check:**
```bash
npm run build && npx vitest run tests/no-pi-bundle.test.ts
```
Must exit 0 with all 3 tests passing.

**PI-07 Certification (two pi versions):**

Run the following on the CURRENT pi version (0.67.68):

1. **Fresh project test (wizard path):**
   ```bash
   mkdir /tmp/mobile-tui-test-fresh && cd /tmp/mobile-tui-test-fresh
   pi install npm:mobile-tui   # or: pi -e /path/to/mobile-tui/src/extension.ts
   /spec
   ```
   Expected: wizard opens, walks through steps, saves SPEC.md, can graduate to canvas.

2. **Existing spec test (canvas path):**
   ```bash
   # Use a project directory that already has a SPEC.md
   cd /path/to/project-with-spec
   /spec
   ```
   Expected: canvas opens with 3-pane layout, screens list shows existing screens, wireframe preview renders.

3. **Handoff test:**
   - From canvas, run `:yank wireframe <screen-id>` — wireframe should be in clipboard.
   - From canvas, run `:prompt screen <id> swiftui` — prompt emitted.

4. **Quit test:**
   - Press `Ctrl+Q` — extension quits cleanly, no errors in pi console.
   - Re-open with `/spec` — canvas resumes from same position.

**On a prior pi version (0.66.x or nearest available):**

Repeat steps 1–4 above with the prior pi version installed. Note the version.

**If any step fails:** Fix the issue, re-run `npm run build`, retry. Document failures and fixes in 09-HUMAN-UAT.md.

**After certification:** Update `.planning/phases/09-pi-dev-integration-packaging/09-HUMAN-UAT.md` with the certification block:
```markdown
## PI-07 Author Certification

**Certified by:** [author name]
**Date:** [date]

### Version 1: pi [version]
- Fresh project (wizard): PASS / FAIL — [notes]
- Existing spec (canvas): PASS / FAIL — [notes]
- Handoff commands: PASS / FAIL — [notes]
- Quit and resume: PASS / FAIL — [notes]

### Version 2: pi [version]
- Fresh project (wizard): PASS / FAIL — [notes]
- Existing spec (canvas): PASS / FAIL — [notes]
- Handoff commands: PASS / FAIL — [notes]
- Quit and resume: PASS / FAIL — [notes]

**HUMAN-GATE: APPROVED** [or: BLOCKED — [reason]]
```
  </how-to-verify>
  <resume-signal>
Type "approved" when PI-07 certification is complete and 09-HUMAN-UAT.md has been updated with both version certs. Or describe any issues found.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README instructions → user execution | README commands are copy-paste runnable; npm package name matches package.json |
| npm publish → pi install | Published package must have correct pi field and peerDependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information Disclosure | README documents internal implementation details | accept | README is public documentation; no secrets or credentials referenced |
| T-09-04-02 | Tampering | npm package name squatting | accept | Package not yet published to public npm; v1 is author-install via `pi -e`; public publish is a post-cert step |
| T-09-04-03 | Denial of Service | pi version incompatibility | mitigate | PI-07 manual cert on two versions confirms compatibility; peerDependencies `"*"` range prevents install failure |
</threat_model>

<verification>
1. `grep "pi install npm:mobile-tui" README.md` — exits 0
2. `grep "/spec" README.md` — exits 0
3. `grep "yank wireframe" README.md` — exits 0
4. `grep "Ctrl+P" README.md` — exits 0
5. `wc -l README.md` — ≥ 60 lines
6. `npm run build && npx vitest run tests/no-pi-bundle.test.ts` — exits 0 (3 assertions pass)
7. `cat .planning/phases/09-pi-dev-integration-packaging/09-HUMAN-UAT.md | grep "HUMAN-GATE: APPROVED"` — present after cert
8. `npx vitest run` — full suite passes
</verification>

<success_criteria>
- README.md covers all D-311 sections: install, /spec flow (both paths), 8 wizard steps by name, canvas keybindings, handoff commands, YAML schema example
- README is ≥ 60 lines of substantive developer documentation
- tests/no-pi-bundle.test.ts passes after npm run build (PI-02 invariant holds)
- PI-07 author certification completed on 2 pi versions — 09-HUMAN-UAT.md has the certification block
- npx vitest run full suite passes (PI-08 all automated requirements green)
</success_criteria>

<output>
After completion, create `.planning/phases/09-pi-dev-integration-packaging/09-04-SUMMARY.md`
</output>
